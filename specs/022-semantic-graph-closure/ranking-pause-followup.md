# 排序超时、预算暂停与停止原因一致性

2026-09-09，针对运行 `4cfb7417-a873-45b4-ae78-a30632523300` 的
`attempted_incomplete / ranking_paused` 报告。本轮没有变更模型、CUDA、开关、
冻结预算、文档或本体，T017 专家质量验收保持开放。

## 根因与证据

06:30:10 UTC 采样时运行已是 running，已提交 4 个 semantic epoch，未降级；
另有 pending epoch `ranking_call_budget_exhausted`。此前的 DataError 已解决，
本次不能再次归因于排序环境缺失。

06:34:35 的预算检查显示，第 5 个 epoch 首先以 `ranking_timeout` 暂停：
25 次预留（1 embedding + 24 reranker，96 个输入对），89,113 tokens，耗时
1,240.968 秒，超过冻结的 1,200 秒。其恢复随后以调用预算耗尽暂停，未增加调用。
每条记录上限为 2，发现/反证双意图正常各占一次；229 是累计预留次数，不是全运行上限。
当时累计 584,414 tokens，未达到每运行 4,194,304 或每槽位 524,288 的限制。

请求前执行顺序为：预扣 → 等待主线程持久化 → 检查期限 → 派发模型。
主线程同时执行识别/验证，可能长时间不能确认排序预扣；等待跨过期限后，
本次没有派发也已占用逐记录额度。实际请求记录中，正常 GPU 精排多数约 0.4–0.9 秒，
请求间隔却达数十秒。预留 229、实际 GPU 完成 228、tokens 差额 3,263，与该路径相符。
旧记录缺少当时的确定未派发回执，不能仅凭请求表缺项追溯释放预算。

另有两处状态问题：运行中批次把尚未结束的覆盖写成 `attempted_incomplete`；
终态复用最后检查点图，导致运行已 `ranking_paused` 而图仍显示旧批次原因。
恢复还可能先选择其他槽位，绕过原有预算暂停并产生新请求。

原始技术证据保存于新目录
[本轮证据](../../evaluations/022-ranking-pause-followup-20260909/)，
旧 DataError 与 CPU/GPU 验收目录均未重写。

## 修复边界

- 新增明确未派发回执：仅在当前进程确认尚未调用模型、持久化等待后已超时时追加。
  同一完整输入可复用原预留，派发前先持久认领；累计费用与次数不回减。
  认领后崩溃、实际请求失败以及旧无回执状态仍保守占用额度。
- 回执链与持久化历史必须合法且只能追加，请求次数、记录次数和槽位 token 账本不能回退。
- 恢复先核验并处理当前有效的暂停排序，再允许新任务；失效主体/计划不阻断有效工作。
- 运行中全局停止原因为 null；终态原因在 API 响应叠加，保留不可变图谱检查点、
  覆盖计数和逐槽位结果。未提交的暂停池不计入实际执行模式。
- 页面直接显示中文预算/超时/技术原因，原始技术码留在诊断中；原文技术未完成与排序暂停分开说明。

## 验证与当前运行

专项覆盖了预扣等待超时、同笔预留单次领取、认领后崩溃、实际派发后超时、旧状态兼容、
回执历史回退、累计账本回退、正常完成/任务预算/排序暂停终态，以及 API 保留检查点。
13 个受影响后端文件组合共 **118 passed**（4 条既有依赖警告，39.39 秒），
前端 `node --test tests/document-ranking.test.mjs tests/document-analysis-runs.test.mjs`
为 **17 passed**，本轮文件 Ruff、ESLint 与 diff 检查通过。
全前端 `tsc --noEmit` 在后续共享工作区检查中，被其他模板编辑中的
`template-slot-editor.tsx` 1405/1442/2375 三处类型错误阻断；本轮未覆盖该文件修复。
验证摘要见 [checks.json](../../evaluations/022-ranking-pause-followup-20260909/checks.json)。

06:51:42 UTC 已通过真实 Chrome + 真实 Compose API 页面检查，未拦截 API、
未保存原文截图/DOM、没有 API 写请求或页面异常；无需展开诊断即可看到中文预算原因。
见 [浏览器断言](../../evaluations/022-ranking-pause-followup-20260909/browser-check.json)。

为重载已修改的后端模块，通过既有 CAS 控制接口软暂停本运行，保留原指纹
`d83702d00a332d1a5c1f9daccb830d150ba6d1bae0ee521e1ed7b6c18fdb4868`。
维护暂停时 tasks_attempted=62、4 个 semantic epoch、排序预留 240、GPU 完成 239；
累计 605,284 tokens。暂停中的一次识别请求取消记录保留，没有清零任何费用。

旧运行没有可领取的未派发回执，修复不会为它补造回执或提高冻结额度。
新实现可避免今后相同等待故障永久占用未执行的预留；这不意味着本次旧运行已完成图谱识别。

## 实际上线与恢复验收

07:02:10 UTC 仅重启 backend 以加载 bind mount 中的代码。重启前 active runs、
有效租约和活动模型请求均为 0，db/frontend/web 身份和启动时间保持不变；
没有重新构建镜像、改配置或执行新迁移。随后代理健康接口 200，并通过新的 CAS
控制键恢复同一运行，验证恢复前检查实际生效。

07:03:58 UTC 真实 API 已一致返回：运行与图谱停止原因为 `ranking_paused`，
具体原因 `ranking_call_budget_exhausted`，实际已提交模式只有 semantic、轮次 4。
任务仍为 62，调度请求总数保持 638，排序预留保持 240 次/605,284 tokens；
原文、本体、元数据、图谱及识别检查点的 revision/hash 完全未变。
复核没有新模型请求，也未接受部分评分作为完整排序。

恢复前检查最终另通过 13 项定向回归（包括 5 项暂停恢复场景），覆盖多项暂停间
不夹带新识别任务。07:05:06 UTC 再次通过真实 Chrome/真实后端的中文原因检查，
仍为零 API 写请求、零页面异常。

- [后端重载](../../evaluations/022-ranking-pause-followup-20260909/backend-reload.json)
- [真实 API](../../evaluations/022-ranking-pause-followup-20260909/public-after-reload.json)
- [恢复验收断言](../../evaluations/022-ranking-pause-followup-20260909/acceptance.json)
- [最终浏览器断言](../../evaluations/022-ranking-pause-followup-20260909/browser-final.json)

公共费用投影继续保留原先的 2 个未计量请求，而调度表有更晚的请求完成计量；
本轮未回填旧观察或声称两者已完全对账。当前运行仍是预算暂停、图谱未完整完成，
不能把本次故障修复验收当作全文识别或 T017 质量验收完成。
