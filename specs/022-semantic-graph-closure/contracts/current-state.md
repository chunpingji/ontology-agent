# 当前状态暂停继续契约

新运行冻结 state_storage_version=4。唯一恢复入口为运行的当前 work_version；读取必须来自同一事务的已提交视图，校验 run 指纹及分区哈希。新格式不读取 task_outcomes 或历史 epoch 顺序重放。公开 API 不增加历史版本选择。

任务提交携带预期工作版本、批次身份及变化分区；协调者在现有执行权锁下读取最新控制/账本，拒绝失租、删除、取消和真实工作冲突，允许暂停安全边界及无关心跳/预扣。完成任务的状态、候选/证明、消费位置、覆盖、工作版本与幂等回执原子提交。

请求预扣与派发认领在调用前持久化，结果保存后才允许消费；新执行代际不重置费用。精排结果依赖精确输入、权限、模型和语义身份，未应用结果继续复用。展示缓存只保留最新，预算变更不重建图；GET 不启动模型。旧格式沿用既有契约。
