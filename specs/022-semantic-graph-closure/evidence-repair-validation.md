# 证据聚合、属性协议及证明菜单修复验证

日期：2026-09-10。依据[优化方案](../../docs/关系谱图识别新内核的性能优化方案.md#820-本轮实施记录证据协议与持久执行)、
[契约](contracts/evidence-repair.md)及[实测预登记](evidence-repair-validation-plan.md)。
本轮工程验证修改代码并执行隔离测试，当时未部署，新增开关代码默认关闭。
2026-09-11随后按用户指令在本机开启新开关并重新部署，见
[部署记录](evidence-repair-deployment.md)；本次部署检查未新增真实模型评测。

最新结论（2026-09-11）：证据聚合、独立属性协议、同源证明菜单及持久恢复已接入共享内核。
修复范围引用、补证接续和整数引用后，v9同版本自动组三轮均形成六个正确目标及两条同父
完整路径，首达428.85–431.37秒，每轮17次请求；定向组三轮36项正例正确、6项日期反例实际拒绝。
本开发样本的目标路径重复验证通过。额外describes仍未评分，全文未完成；没有旧内核同预算
对照或独立专家金标，因此不宣称全图质量达标、统计提速或全部优化方案完成。
下文保留v5跨表误挂、v6设备未决及v7/v8整数失败的历史证据，不用最新成功覆盖旧失败。

## 输入与口径

- 原件：`1、HRS-5592原料临床备样生产信息表-DP-C-PI-S-X21372 2602 01 - 删减结构式供外版.docx`。
- 原件 SHA256：`2c1174bf616f30dd28c655fef4261c167762de807c8ad79e148fb8ef18e16436`。
- CMCReport 本体快照 hash：`25a0576f6f6d0efb002c44c5449dfba4329144da3868f9b6234a4038d3d68b6c`；未修改本体。
- 本机模型：Qwen3.6-35B-A3B；revision `0b21525e972670ed59e1812e170b27c26355381f0656ecc4e25617ece7dac58b`。
  温度0.1，识别在途1，llama_server tokenizer，输入32768、输出20480 tokens，单请求超时600秒。
- 所有组关闭语义排序，故不验证真实 embedding/reranker 或语义扩搜。
- 每次独立 run/SQLite 调度库/代码及本体快照。模型调用写入调度与费用记录，不是只读操作。
  记录运行前环境与服务 slots 前后状态；自动组另有运行后环境快照。
  复用本机热服务，没有强制清空模型缓存，耗时不得视为冷启动对照。

原文核对范围是生产计划关系及计划日期、批次数，以及设备关系及编号、规格型号。
定向组固定首行1000L反应釜；自动组不指定设备记录，按实际首达设备所在表格行核对两项属性。
P26明确写“计划在2026年08月……本次计划生产1批”；文档日期“2026年07月”不是计划日期。
设备表 table:9 的首数据行：名称“1000L反应釜”、编号“RE64615/RE64215”、规格型号“1000L”。
编号原值整体保留，不拆设备或误当型号。参考仅用于结果核对，不进入识别提示、检索词或模型配置。

根层为287条逻辑记录 × 12条合法关系 = 3444个覆盖机会。启发式政策保留全集，先准入小批候选；
子实体出现后新增义务单独入账。检查机会不是已创建任务数，也不是模型请求数。

## 工程证据

新增回归覆盖：互补A+B、同证据零重验、冻结发现补证只新增核验；发现/响应落盘后的故障恢复；
字段错列、换列顺序、同名异行设备漏原主体引用、假条件、必需 facet 与实际请求schema；
重复数字定位、原值/规范值分离；迟到未证实反证暂停旧路径；额度耗尽恢复已付费结果。
同名异行反例先复现2项失败，再补主体来源门，未放宽断言；受控成功响应同步补齐两侧原文引用。

状态存储的200多个块用例验证批量恢复 SELECT 少于12次，保留hash、所属运行及水位检查。
此为隔离查询数量上界，不是生产整轮SQL或保存耗时指标；全量编码CPU和历史 inventory 扫描仍存在。

截至v6，共享核心、API及审计入口组合 **254 passed**，含新增归属门与审计检查。
跨行同名、跨表同名、备注作主体三个反例在修复前实际失败，修复后与合法同一行正例一起通过；
正文明确主谓句、跨记录补证和恢复测试仍通过，未放宽断言。
专用PostgreSQL组合 **12 passed**、0 skipped。
前端 `tsc --noEmit`、定向 ESLint 和3项Node测试通过。最终改动Python文件Ruff通过；
没有执行全库测试、生产构建或真实浏览器端到端验收。pytest仅出现已有Starlette/Pydantic兼容性警告。

后端实际组合（工作目录backend）：

```bash
.venv/bin/python -m pytest -p no:cacheprovider -q \
  tests/test_extraction/test_evidence_repair.py \
  tests/test_extraction/test_evidence_repair_cost_audit.py \
  tests/test_extraction/test_heuristic_search.py \
  tests/test_extraction/test_document_state_artifacts.py \
  tests/test_extraction/test_ontology_guided_boundaries.py \
  tests/test_extraction/test_ontology_guided_core.py \
  tests/test_extraction/test_ontology_task_citations.py \
  tests/test_extraction/test_independent_semantic_verification.py \
  tests/test_extraction/test_semantic_proof_regressions.py \
  tests/test_extraction/test_semantic_pause_recovery_regressions.py \
  tests/test_extraction/test_semantic_model_call_budget.py \
  tests/test_extraction/test_semantic_proof_persistence.py \
  tests/test_extraction/test_non_llm_performance.py \
  tests/test_extraction/test_scheduler_performance.py \
  tests/test_api/test_document_analysis_performance.py \
  tests/test_extraction/test_document_analysis_execution_recovery.py \
  tests/test_extraction/test_recognition_coordinator.py \
  tests/test_extraction/test_semantic_subject_versions.py \
  tests/test_extraction/test_ontology_guided_node_revisions.py \
  tests/test_extraction/test_semantic_scheduler.py
```

PostgreSQL使用本轮新建专库
`postgresql+psycopg2://postgres@127.0.0.1:55440/document_analysis_evidence_repair_20260910_test`，
运行 `test_evidence_repair_postgresql.py`、`test_performance_postgresql.py`、
`test_document_run_execution_postgresql.py`。以当前metadata建表并stamp到0035；这不是迁移链通过证据。
Ruff使用 `check --no-cache`，前端运行文件为 `document-analysis-runs.test.mjs`、
`document-ranking.test.mjs`、`template-document-performance.test.mjs`；不启动生产服务。

## 历史状态回放（非本轮模型识别）

实际命令：

```bash
.venv/bin/python scripts/benchmark_document_state.py \
  --input /tmp/ontology-performance-20260910/baseline.jsonl.gz \
  --output-dir /tmp/ontology-evidence-repair-state-replay-20260910 --samples 30
```

[机器可读结果](evidence-repair-evidence/state-replay.json)：138版（116排序、11图谱、11检查点）
全部恢复载荷及公开投影等价，零模型调用；10350块与检查点合计27,177,787字节，公共读取制品39,404字节。
编码/写块函数累计419.77秒，解码累计131.98秒；30次服务读取中位7.80ms、P95 8.40ms。
编码计时包含写块SQL，不是纯CPU时间。此回放与本机模型诊断有时间重叠，不是独占硬件基准，
也不是线上PostgreSQL、HTTP或浏览器指标。结果支持恢复等价，不能证明第4.4节固定增量CPU目标已达成。

## 诊断运行

早期 `/tmp/ontology-evidence-repair-hrs5592-20260910-d1` 至 `d8` 保留原状。
d1为沙箱网络不可用、零模型请求；d2–d8依次暴露输入重复、提前空候选、引用漏项、条件乱填及短值定位问题。
这些运行伴随代码修订，不能合并为固定代码的重复验收或挑最好结果。

后续 `/tmp/ontology-evidence-repair-hrs5592-20260910-r1`：14个定向任务，32次请求，识别800.24秒。
计划两臂均得到正确日期/批数，两臂均实际拒绝文档日期；设备两臂关系通过，current编号与型号通过，
joint编号因漏当前行主体引用未决。此轮之后进一步修复主体两侧引用，因此仍记诊断。
曾准备的 v2 目录均未执行，不算独立重复运行。

自动v3-r1作为诊断：24个派发批次、40次请求、952.91秒，覆盖20/32144个机会（包含新增主体义务）。
发现离心机与编号正确，但把“目前项目处于I期临床阶段”“属于肿瘤药产品”建成产品实体，
且启发式普通任务丢失模板优先标记，计划路径未达。随后补指称形态门和普通任务优先/恢复，
不将v3追认为固定代码通过组，其余v3准备目录未执行。

自动v4-r1在召回缺口定位后显式中止：识别592.69秒，9个已提交批次，18次主请求预扣；
最终状态为KeyboardInterrupt诊断，图谱仍停留在此前17次请求的提交水位，不能把差额记成零费用。
设备两项已有部分正确结果，但计划未准入。根因是“含备样生产计划”和“计划在……完成生产”
没有整词命中。随后补受约束词片H1、已证明主体原文种子及语义关闭时即时扩搜；
用相同原件/本体作无模型预检，计划P26排在H1首位。其余v4目录未执行。

## 固定代码真实组

v5同组预先冻结3个独立run，完整制品位于：

- 定向组：`/tmp/ontology-evidence-repair-directed-hrs5592-20260910-v5-r{1,2,3}`。
- 自动组：`/tmp/ontology-evidence-repair-auto-hrs5592-20260910-v5-r{1,2,3}`。

定向组每轮32任务/48请求/1200秒，每lineage8请求；current/joint均使用新协议，区别是固定辅助来源，
不是新旧内核对照。使用共享工单反馈最多重新提议一次，保留初次outcome。
自动组每轮24任务/48请求/1200秒/最多4跳，每lineage8请求，完整菜单与全集保留；
优先路径仅指定本体谓词 usesEquipment→equipmentID/modelSpecification、
hasProductionPlan→plannedProductionDate/plannedBatchCount，没有注入原文记录编号。
自动组冻结 `stop_after_focus_paths=true`：同一设备的两项属性和同一计划的两项属性均进入
有效投影后，或触及预算时停止。此条件不检查答案，不证明业务正确或全文完成；后续按原文核对。
因此总耗时仅代表本次计时实验，不据提前停止宣称整份报告提速。

v5六轮已全部执行。自动组第三轮发现真实归属漏检，后续已修复；因此v5是修复前固定组，
不能称为最终代码全部通过。结果同时保留成功、未决、未执行及归属未证实项；未展开子任务不算反例通过。
定向任务秒数包括该任务内部重新提议，节点首达为从该轮识别开始到有效投影的累计秒数，二者分列。
自动组阶段、保存与SQL计时可能重叠，不能相加为总时间；JSON实验保存不等同生产PostgreSQL制品写入。

已用 `backend/scripts/summarize_evidence_repair_runs.py --directed DIR1 DIR2 DIR3
--automatic DIR1 DIR2 DIR3 --output NEW.json` 汇总。该工具要求六个不同运行身份，核对实际冻结文件、
组内预算及跨组共享识别代码/本体/模型一致性；保留每批候选（含被拒绝/未决的旧版本）、
最终候选及未执行范围，不自行给模型断言评分。费用同时保留服务返回的prompt/completion tokens、
输入处理/生成毫秒数、缓存token及未知计数字段；缺测不能记为实际零成本。

跨组配置审计发现一个未启用参数不同：定向组 `semantic_ranking_pool_size=64`、自动组16，
两组均明确关闭语义排序。审计保留该差异，只允许此未启用参数跨组不同；模型revision或
启用状态漂移仍拒绝，组内全部文件仍逐个核验hash。原始冻结目录未改写。

### 定向组：三轮均完成正例和实际反例检查

三轮均14任务、32次请求，其中每轮12个正例任务正确、2个文档日期反例被实际拒绝。
合计36/36正例任务正确、6/6日期反例实际拒绝，父关系阻塞的未执行子项为0。
两个臂的任务数不等同不同业务事实数；每轮核对的是相同六个目标在两个上下文臂中的结果。

下表每格为**单任务耗时 / 从本轮识别开始累计结束时间**，单位秒。反例累计时间是拒绝时刻，
不是正确属性首达；定向任务顺序人为固定，不应与自动调度首达直接比较。

| 节点/检查 | 臂 | r1 | r2 | r3 |
|---|---|---:|---:|---:|
| 生产计划关系 | current | 32.77 / 32.77 | 32.17 / 32.17 | 32.22 / 32.22 |
| 生产计划关系 | joint | 29.42 / 62.19 | 29.31 / 61.48 | 29.22 / 61.44 |
| 计划日期 | current | 30.65 / 92.85 | 30.48 / 91.96 | 30.38 / 91.83 |
| 计划批数 | current | 26.82 / 119.68 | 26.01 / 117.98 | 26.46 / 118.30 |
| 错误文档日期反例 | current | 63.70 / 183.38 | 63.20 / 181.18 | 63.58 / 181.88 |
| 计划日期 | joint | 29.70 / 213.08 | 28.94 / 210.13 | 28.65 / 210.53 |
| 计划批数 | joint | 28.08 / 241.16 | 27.77 / 237.90 | 28.22 / 238.76 |
| 错误文档日期反例 | joint | 65.59 / 306.76 | 65.52 / 303.43 | 66.30 / 305.06 |
| 设备关系 | joint | 101.16 / 407.92 | 99.12 / 402.55 | 100.39 / 405.46 |
| 设备关系 | current | 92.59 / 500.52 | 93.12 / 495.68 | 93.99 / 499.46 |
| 设备编号 | joint | 83.01 / 583.53 | 82.47 / 578.15 | 82.33 / 581.80 |
| 设备型号 | joint | 78.47 / 662.00 | 79.31 / 657.46 | 78.91 / 660.71 |
| 设备编号 | current | 75.54 / 737.55 | 77.29 / 734.76 | 75.62 / 736.33 |
| 设备型号 | current | 75.74 / 813.29 | 76.60 / 811.36 | 75.92 / 812.26 |

这证明新协议能提取并挂接所测字段，不能证明人工增加辅助来源必然提速。
current/joint都使用新协议，且本组并非自动跨记录补证的独立收益实验。

### 自动组：正确首达与系统误判分别记录

单位秒，从各轮识别开始累计；“未达”不能排除出样本或填成0。

| 节点 | v5-r1正确首达 | v5-r2正确首达 | v5-r3正确首达 |
|---|---:|---:|---:|
| 使用设备关系 | 95.82 | 96.10 | 450.53 |
| 该设备编号 | 171.65 | 172.72 | 未达（系统曾在553.09秒误判） |
| 该设备型号 | 469.68 | 468.95 | 未达（系统曾在646.67秒误判） |
| 生产计划关系 | 204.23 | 205.43 | 220.77 |
| 该计划日期 | 233.95 | 236.34 | 358.41 |
| 该计划批数 | 398.04 | 397.72 | 384.75 |
| 两条同父完整目标路径均正确 | 469.68 | 468.95 | 未达 |

r1/r2设备为table:9 row3的离心机，编号`CT64611/CT64214`、型号`800mm`均回放到同一行。
r3设备为清洁表table:8 row7的真空干燥箱，模型把设备表table:9 row30的`DE64603`、`24盘`
挂给它；两表同名不证明同一物理设备。编号局部主体引用甚至来自row30 col12备注，
不是设备名称列。两项均判为归属未证实，不能将最终非空图或提前停止记为正确路径。

| 运行 | 识别总秒数 | 派发批次 / 主请求 | 首次检查 / 全部机会 | 目标核对 | 额外系统有效但未评分 |
|---|---:|---:|---:|---|---:|
| v5-r1 | 470.00 | 10 / 19 | 8 / 18655 | 六项正确，两路径完整 | 1 |
| v5-r2 | 469.45 | 10 / 19 | 8 / 18655 | 六项正确，两路径完整 | 1 |
| v5-r3 | 647.19 | 12 / 23 | 10 / 18655 | 四项正确，两项归属未证实 | 2 |

三轮均全文`incomplete`，实验停止条件为目标路径进入系统有效投影；运行公共停止原因为
`adaptive_search_saturated`。二者均不表示全文完成。r1/r2还有2个未生效历史候选版本，
包括清洁关系/状态句产品候选；r3还有未决设备候选。逐批候选状态保留在指标制品中。

额外系统有效项也未隐藏：r1的`describes → ClinicalTrialDrug`、r2的`describes → DrugProduct`，
r3的`describes → ClinicalTrialDrug`及`currentDevelopmentStage`均单列未评分。
原文为原料药，本体DrugProduct定义涉及finished dosage form，类型张力尚未解决；
六个目标正确不等于全输出precision为100%。未执行的错列/错日期分支不算自动反例通过。

### 归属修复与新运行：零误挂回放不代替路径成功

增加`source-owned-binding-v2`后，表格/字段组须回到已确认主体的精确物理来源，
或提供显式可核验指代链；跨行同名、跨表同名、备注作主体不能凭双侧引用通过。
正文明确主谓句保留独立语义核验。新政策进入模型身份、运行政策及阶段状态；
旧归属版本缓存结果不得静默恢复为新策略通过。

对v5保存的12条真实属性响应执行新增归属门回放：10条通过、2条跨表挂接被拒绝，
**0次模型调用**。这是确定性归属校验的回放，不是全流程重新识别或暂停恢复验收。
原始/局部引用依旧schema的规范化角色顺序拆分，候选通过proof target映射，原文从原件IR重建。
新增请求已分别保存`original_subject_support`和`local_subject_support`，无需依赖合并顺序。

修复后另开真实自动运行`/tmp/ontology-evidence-repair-auto-hrs5592-20260910-v6-owner-r1`，
同样24任务/48请求/1200秒及4跳预算；冻结文件全部hash通过，冻结应用代码与交付工作区一致。
此为修复后的**单轮补充验证**，不能与v5组成最终代码的三轮成功组。

| v6节点 | 正确首达秒数 | 结果 |
|---|---:|---|
| 生产计划关系 | 223.78 | 原文P26通过 |
| 计划日期 | 361.00 | `2026年08月` → `2026-08` |
| 计划批数 / 计划完整路径 | 387.22 | `1` → 整数1，同一计划主体 |
| 使用设备 / 编号 / 型号 | 未达 | 设备关系未决，编号和型号任务未执行 |

该轮1196.78秒、24批、45请求，因`task_budget_exhausted`结束，无技术异常；
覆盖16/23534个机会，23518未尝试、6个未决声明、413个待执行前沿，全文未完成。
首次检查16与派发24的差额来自重新提议，不是额外检查8份原文；主请求均已结算，无未决费用。
得到3个经目标原文核对的有效断言，另4项系统有效但未评分：两个类型不同的`describes`、
`currentDevelopmentStage`和`registrationCategory`。不能据这些额外输出宣称全部正确。

设备未达的直接原因是模型把设备类IRI填入`applicability.equipment`，给无条件原文添加
不存在的适用范围。离心机在97.6/194.8秒、真空干燥箱在452.9/520.3秒均因此未决；
后续设备探索在1039.9/1137.7秒仍有同类问题。有界重新提议已实际执行，但没有纠正。
因此本轮没有形成设备属性候选，**不能把没有误挂输出当作新归属门的在线反例通过**。
拦截归属漏检的实证来自上述12条响应回放与修复前失败、修复后通过的工程反例。

### 关键阶段与费用

下表均为各轮实际观测；prefill/生成计时来自本机服务返回，属于请求内子计时。
prompt tokens是服务返回值；另一`input_tokens`字段全缺测，制品记录null，不能写成零输入。

| 运行 | 请求数（发现/核验） | prompt / completion tokens | 请求累计秒 | prefill秒 | 生成秒 |
|---|---:|---:|---:|---:|---:|
| 定向v5-r1 | 16 / 16 | 168498 / 9566 | 809.99 | 623.27 | 172.14 |
| 定向v5-r2 | 16 / 16 | 167909 / 9563 | 808.10 | 621.65 | 172.49 |
| 定向v5-r3 | 16 / 16 | 168151 / 9535 | 808.83 | 622.51 | 172.50 |
| 自动v5-r1 | 10 / 9 | 93514 / 5397 | 457.53 | 353.37 | 97.28 |
| 自动v5-r2 | 10 / 9 | 93177 / 5416 | 456.63 | 351.73 | 97.89 |
| 自动v5-r3 | 12 / 11 | 129874 / 7773 | 632.06 | 487.31 | 135.62 |
| 自动v6-owner-r1 | 24 / 21 | 238261 / 15464 | 1165.62 | 870.27 | 276.58 |

| 自动运行的阶段/制品 | v5-r1 | v5-r2 | v5-r3 | v6-owner-r1 |
|---|---:|---:|---:|---:|
| 启发式索引秒 | 0.193 | 0.187 | 0.185 | 0.192 |
| 全部槽位检索准备秒 | 0.976 | 0.914 | 0.954 | 0.948 |
| 上下文装配秒 | 0.089 | 0.084 | 0.123 | 0.179 |
| 输入分词秒 | 0.754 | 0.831 | 1.005 | 1.709 |
| 结果应用秒 | 2.111 | 2.141 | 2.262 | 2.668 |
| 模型调度排队累计秒 | 0.286 | 0.274 | 0.283 | 0.499 |
| checkpoint次数 / MB / 秒 | 10 / 125.10 / 2.224 | 10 / 125.13 / 2.279 | 12 / 160.86 / 2.790 | 24 / 392.65 / 6.601 |
| model-state次数 / MB / 秒 | 77 / 16.12 / 0.386 | 77 / 16.21 / 0.445 | 93 / 18.59 / 0.498 | 183 / 56.42 / 1.322 |
| 调度SQLite识别SQL / SELECT | 310 / 57 | 308 / 57 | 389 / 69 | 748 / 135 |

MB为十进制字节换算。这里的保存为实验JSON写入，SQL为隔离模型调度SQLite；
不能与旧生产状态库72.5万SELECT直接作降幅对照。完整生产PostgreSQL识别性能本轮未测。
各阶段、请求及保存可能重叠，不能把表中各项相加当作总耗时。

v5自动组H0/H1分别负责整词和受约束词片/原主体来源种子准入，H2/H3未触发；
v6在548.21秒起观察到H3继续探索，H2仍未触发。所有运行语义排序关闭。
实际v5补证重验为0，r1/r2各有2次重新提议；v6有8次重新提议，未发生补证重验。
“同证据零调用、补证只重验”的费用收益由工程测试证明，不冒充这些真实运行的已节省调用数。

观测显示启发式定位已较轻；自动v5-r1模型请求457.53秒，其中prefill353.37秒，
修复后单轮模型请求1165.62秒，其中prefill870.27秒。剩余主要成本包含完整本体菜单/原文
重复输入与未决候选反复提议；只压缩检索耗时无法解决关系条件乱填及类型归属问题。
应继续按冻结参考评估输入去重、条件维度表达及未决候选处理，不以删去真实条件换取通过。

### 可核验制品与验收状态

- [v5六轮指标](evidence-repair-evidence/v5-six-run-metrics.json)：逐任务/节点时间、模型费用、
  全部逐批候选状态、未评分项、覆盖及同父完整路径时间；按六个独立身份汇总，失败项未排除。
- [归属门回放](evidence-repair-evidence/owner-guard-replay.json)：12条真实响应的坐标与判定，零模型。
- [v6单轮指标](evidence-repair-evidence/v6-owner-run-metrics.json)：修复后真实运行独立保存，
  设备关系及属性首达为null，保留未决/未执行范围及新代码冻结身份。

Git内制品只保存指标和必要坐标；原件、完整原文、响应、检查点和隔离运行库留在上述/tmp目录，
未复制到历史冻结评测目录。完整私有汇总为`/tmp/ontology-evidence-repair-v5-six-run-audit.json`；
原文核对为`/tmp/ontology-evidence-repair-v5-source-review.json`与
`/tmp/ontology-evidence-repair-v6-owner-source-review.json`。原文核对独立读取DOCX XML，
不采用模型自身的supported作为正确性判据，也不是外部专家裁决。

截至v6，工程层面的协议、来源门禁、费用与恢复验证通过；真实自动完整路径的稳定性门未通过，
ER05当时保持未勾选。不能把v5两次成功或v6计划路径成功概括成优化目标全面达成，
也没有同预算旧内核对照来计算统计提速倍数。

## v6后继续实施与验证

本次继续处理三个已定位的失败模式，所有旧运行原样保留：

1. 发现阶段的自由范围字符串会生成本体IRI或虚构范围。改为`scope_qualifiers`逐字条件引用，
   保留真实条件，引用仍须独立语义核验；相同物理端点不自动成为额外条件。
2. 原工单在任一种重试完成后直接返回，阻止另一种合法修复。新工单v2分别保留一次重新提议
   和一次新证据重验，可按两种顺序接续；暂停恢复不刷新计数或原lineage费用。
3. v7诊断中的计划批数引用了“1批”，本体要求整数。增加单一已解决整数类型的原文词面候选，
   保留数字子串及全文角色引用；datatype_mismatch允许一次新提议，不自动剥单位改写原值。

上述scope/literal/work版本进入新运行身份，未知或缺少版本时在构造适配器之前明确拒绝；
保存的阶段响应也检查对应版本。旧默认协议不自动启用新策略。

新增范围用例最初4项失败；工单双向接续的4项用例最初全部失败，修复后通过。
整数引用v1阶段的完整组合为前述20文件加`test_evidence_scope_protocol.py`、`test_evidence_integer_quotes.py`、
`test_evidence_work_continuation.py`，实际 **273 passed**；同一专用PostgreSQL三文件组合再次
执行，**12 passed、0 skipped**。改动Python定向Ruff和`git diff --check`通过。
本次继续未修改前端，没有重复运行上轮已通过的前端检查；工程通过不等于模型质量通过。

v7-scope-d1仅包含范围修复；它与最终代码的工单接续及整数协议不同，作为诊断单列。
该轮1222.31秒、23批、39请求，实验按`experiment_deadline_reached`结束；已派发请求完成后
才停止，因此超过1200秒软限额22.31秒。公开停止原因是`evidence_recheck_incomplete`，
全文incomplete；覆盖18/32144，32126未尝试，6个未决声明。
设备关系/编号/型号正确首达95.88/171.76/430.51秒，生产计划/日期205.61/235.95秒；
计划批数357.9秒引用“1批”，类型不符而未决，不能记正确首达。实际补证重验5次，
仍未修复该原值格式，说明类型格式错误应先重新提议，不能仅扩大原文。

该轮另有真空干燥箱关系声明为Dryer。最初审阅脚本只接受精确VacuumDryer，误标失败；
冻结本体明确`VacuumDryer rdfs:subClassOf Dryer`，既有契约允许合法父类声明，
故修正独立审阅为按冻结本体检查祖先类型，单列supported_ancestor。没有放宽来源、方向、
条件或owner检查；旧审阅JSON保留。最终6条关系/属性断言通过原文核对，覆盖5种目标谓词，
另2个describes仍未评分，plannedBatchCount仍缺失。不能把“6条”当作六个目标全部达到。
见[v7诊断指标](evidence-repair-evidence/v7-scope-diagnostic-metrics.json)。

v8定向和自动各3个新运行在执行前冻结；组内及跨组应用、本体和模型配置完全一致。
自动首轮发现计划批数引用失败后，剩余五轮在派发前停止，不能按六轮完成组汇总。
首轮全部冻结文件hash再次核验，模型请求和费用完整落盘；暂停的串行派发器在子进程结束后
已终止。v8使用同一份16池配置且语义排序关闭，没有v5跨组未启用池大小差异。

### v8整数引用失败与v9修复诊断（2026-09-11续）

v8-auto-r1耗时1222.73秒、24批、39请求（发现21/核验18）；按实验时限停止，
公共原因`task_budget_exhausted`，全文incomplete。检查16条、技术未完成1条、未尝试32127条，
总机会32144，未决3项。请求传输完成不代表引用解码成功。

| v8正确目标 | 首达秒数 |
|---|---:|
| 使用设备关系 | 91.04 |
| 该设备编号 | 166.59 |
| 该设备型号 / 设备完整路径 | 414.73 |
| 生产计划关系 | 199.55 |
| 该计划日期 | 229.84 |
| 计划批数 / 计划完整路径 | 未达 |

计划批数在331.10/339.53秒两次`discovery_citation_invalid`，尚未形成属性候选。
原runner没有保留解码前JSON，因此不能把具体坏引用归因为null定位，只能确认引用协议失败。
该轮6条目标范围断言通过原文/归属核对，覆盖5种谓词，另3个describes未评分。
除正确设备路径外，实际拒绝了跨来源的设备属性尝试；坐标及逐批状态均保留，未把这些拒绝
计入六个正例目标。见[v8单轮指标](evidence-repair-evidence/v8-integer-diagnostic-metrics.json)。

确定性反例使用“在611号车间生产，本次计划生产1批，复核批数2。”：旧schema分别枚举
来源、数字和定位短语，允许`text=1`与null定位组合，但源中多个“1”不能唯一回放。
`source-integer-quotes-v2`改为完整引用对象分支，先验证每个来源/数字/短语组合可唯一回放；
只有数值在该授权来源中已唯一才允许null定位，不根据属性名补造值或取消独立角色核验。
新增反例先失败后通过，最终同一23文件组合 **274 passed**，4个已有警告；定向Ruff与
`git diff --check`通过。本次v2没有数据库写入变更，未重复执行v1阶段已通过的12项PG测试。

新`v9-integer-d1`自动诊断使用独立目录、相同原件/本体/模型/预算，保存解码前JSON与具体
引用失败原因到私有制品；识别不读取该制品。该轮428.67秒、9批、17请求（发现9/核验8），
全部冻结文件hash通过，17条解码前响应保存，费用全部结算。六个目标经DOCX XML原文、
字段列和主体来源核对通过，另一个describes未评分。

| v9诊断正确目标 | 首达秒数 |
|---|---:|
| 使用设备关系 | 95.49 |
| 该设备编号 | 171.32 |
| 该设备型号 / 设备完整路径 | 428.37 |
| 生产计划关系 | 204.36 |
| 该计划日期 | 234.88 |
| 计划批数 / 计划完整路径 | 356.92 |

两条完整路径均正确的首达为428.37秒；计划批数逐字数字与其定位短语通过解码和独立核验。
该轮首次检查8/18655个机会、18647未尝试，全文incomplete。按目标路径实验条件提前停止，
公共停止原因为`adaptive_search_saturated`，不是全文识别耗时。累计模型请求417.24秒，
prefill327.41秒、生成84.04秒；prompt85655、completion4674 tokens，input_tokens字段缺测。
计时可能重叠，不能相加。见[v9诊断指标](evidence-repair-evidence/v9-integer-diagnostic-metrics.json)。

诊断不与历史版本混组，也不计入后续三轮。同版本`20260911-v9-final-r1/r2/r3`定向/自动组
已预先冻结且串行执行完毕，六轮结果及原文审阅见下节。最初漏带原文件名的准备目录
均未执行，0次模型调用；新自动组已显式冻结原始完整文件名。

原文审阅已整理为可重复执行的`backend/scripts/review_hrs5592_evidence_repair.py`，独立读取
DOCX XML，另检查对象/属性的实际引用子串与原值一致；不以坐标未越界或模型supported代替。
定向正例失败、负例未实际拒绝及未执行任务保留，不再因旧临时脚本的成功断言而中断审阅。
新增5项审阅反例，与已有8项费用审计一起运行：**13 passed**；Ruff通过。
该工具没有进入应用或冻结识别输入；对v9诊断的严格引用回放仍为6项通过、1项未评分。

额外对定向v9-final-r1的4条实际请求作离线体积检查，未新增模型调用。
设备joint发现/核验的user JSON分别29177/31310字节，其中field_bindings每次14124字节，
predicate每次6668字节；对应schema为5203/8265字节。计划批数current的user JSON仅3813/5854字节，
schema为3540/7259字节。这里计量UTF-8字节，不能当作tokens或已证明的压缩收益。
结合诊断中327.41秒prefill，后续可优先评估字段映射与本体菜单的重复传输成本；
本组没有改变这些输入，不把体积观察冒充优化实验。见[请求体积制品](evidence-repair-evidence/v9-request-size-profile.json)。

### v9同版本自动三轮

三轮自动组已完成且冻结文件核验通过；每轮六个目标均经原文、字段与主体归属审阅通过，
另各1项describes未评分。以下是正确首达时间，单位秒；诊断轮不计入这三轮。

| 目标 | r1 | r2 | r3 | 中位数 |
|---|---:|---:|---:|---:|
| 使用设备 | 95.62 | 96.34 | 96.67 | 96.34 |
| 该设备编号 | 172.09 | 172.64 | 172.26 | 172.26 |
| 该设备型号 / 设备完整路径 | 429.49 | 431.37 | 428.85 | 429.49 |
| 生产计划 | 206.01 | 206.26 | 205.34 | 206.01 |
| 该计划日期 | 236.19 | 236.58 | 235.45 | 236.19 |
| 该计划批数 / 计划完整路径 | 358.28 | 359.00 | 356.45 | 358.28 |

三轮两条同父完整路径均正确，首达428.85–431.37秒，中位数429.49秒。每轮9批/17请求，
首次检查8/18655个机会，18647未尝试，全文incomplete。识别总时长分别429.79/431.67/429.16秒，
均按目标路径实验条件结束。该结果支持本开发样本目标路径的重复可用性；仍无同预算旧内核
对照、独立保留文档或专家金标，不计算统计提速倍数，也不代表全图precision/recall。
每轮H0关联11次实际请求、H1关联6次请求；H2/H3未触发，语义排序关闭。
因此这三轮验证了启发式词面检索、主体来源种子与识别协议，不冒充语义扩搜验收。
定向组三轮均已完成：每轮14项任务、32次请求，12项正例通过原文审阅、2项错误日期实际拒绝；
共36项正例正确、6项反例实际执行，没有阻断子任务或漏项。两种证据分支是定向协议验证，
不能将其总时长与自动组当成检索速度对照。

### v9六轮费用、阶段及验收状态

六轮应用、本体和模型配置的冻结文件均通过hash核验，六个run身份独立；跨组配置差异为空。
原文审阅脚本版本/hash也完全一致。全部147次请求均有完成及费用记录；input_tokens字段
依旧缺测，prompt_tokens来自服务实际返回。新整数协议实际返回了完整引用组合，
未发生整数引用技术失败；旧失败记录保留，不合并为v9的重复样本。

| 运行 | 总秒数 | 请求数 | prompt / completion tokens | 请求累计秒 | prefill秒 | 生成秒 |
|---|---:|---:|---:|---:|---:|---:|
| 定向r1 | 809.48 | 32 | 169611 / 9301 | 806.25 | 625.50 | 166.54 |
| 定向r2 | 808.64 | 32 | 169974 / 9260 | 805.54 | 625.26 | 166.60 |
| 定向r3 | 814.70 | 32 | 170086 / 9343 | 811.30 | 628.88 | 168.35 |
| 自动r1 | 429.79 | 17 | 85924 / 4675 | 418.17 | 327.77 | 84.62 |
| 自动r2 | 431.67 | 17 | 86132 / 4664 | 419.68 | 329.67 | 83.93 |
| 自动r3 | 429.16 | 17 | 85937 / 4641 | 417.55 | 328.06 | 83.48 |

自动组的启发式索引0.194–0.203秒、全部槽位检索准备0.863–0.941秒、上下文装配0.084–0.086秒，
输入分词0.629–0.726秒、结果应用1.971–2.146秒、模型排队累计0.250–0.276秒。
每轮9次checkpoint累计写114.73–114.76MB、耗时2.01–2.13秒；69次模型阶段/预扣状态写入
14.06–14.21MB、耗时0.35–0.38秒。调度SQLite识别SQL为278/280/280条，其中SELECT均51条。
这些是实验本地JSON与模型调度库指标，不能与旧生产状态库72.5万SELECT直接作降幅比较；
生产PostgreSQL整轮识别性能未测。各阶段及请求子计时存在重叠，不能相加。

实测支持“有界启发式定位后，通过独立证明形成正确关系和属性”的开发样本目标，
先前范围及整数协议阻断已在同版本三轮中消除。主要剩余耗时仍是模型请求及输入处理。
ER05的指定样本验收完成；T017专家/独立文档质量门、ER06完整状态增量、ER07全部停放类型
继续游标及纯检索反证订阅仍未完成，未部署，新开关仍默认关闭。

完整指标见[v9六轮实测制品](evidence-repair-evidence/v9-six-run-metrics.json)：包含六轮费用、
正确首达、同父完整路径、逐批候选状态、覆盖和未评分项。完整原件/响应/运行库及独立审阅
留在`/tmp/ontology-evidence-repair-{auto,directed}-hrs5592-20260911-v9-final-r{1,2,3}`；
私有费用汇总与审阅为`/tmp/ontology-evidence-repair-v9-final-six-run-audit.json`和
`/tmp/ontology-evidence-repair-v9-final-source-reviews.json`，未改写历史冻结目录。

## 剩余边界

本轮尚不能替代T017专家参考及独立文档质量门，也不据单一样本宣称完整识别或统计提速。
完整增量事件/周期基线、全部停放类型的继续游标、纯检索线索反证订阅和一般多值基数等目标未全部落实。
未知单位换算和未解决约束保持未决。生产接入代码存在与已部署/已开启是不同状态。
