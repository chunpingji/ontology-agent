# 实现计划

1. 已从 Pencil 核对文案、结构及样例，用户已澄清为静态 Mock。
2. 复用 dashboard layout、导航模型和 UI 组件；新增 `/mock/gxp-process` 页面及独立 Mock 数据/组件。客户端状态保存各操作的编辑和会话草稿，刷新恢复示例。
3. 按类型展示规程要求，以弹窗处理新增和编辑。结果初始状态为未判定，提交仅模拟待审核。
4. 执行定向 ESLint、TypeScript 与浏览器验证，核对导航、编辑隔离、只读、响应式和无业务请求。

宪章核对：无公共 API、数据库、本体或真实审核变更；不新增依赖及运行时外网访问。原型依据和验收先于实现，保留用户已有工作区改动。
