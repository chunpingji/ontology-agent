# 025 指定模板 Finder 图谱演示

状态：本地工程实现与验证完成，已部署至现有开发环境；实际绑定/原件及专用 PostgreSQL 并发验收待补充。依据：[实施方案](../../docs/模板级Finder图谱演示实施方案-20260914.md)；测试和部署结果见 [validation.md](validation.md)。

## 需求与澄清

2026-09-14 用户授权隔离移植 b6f9bd4 Finder，仅恢复指定模板图谱、属性、原文；模板页和报告中心共用模板上下文。schema V1/V2 与识别模式独立。默认空绑定，目标 ID、原件和关键展示项待用户提供。

- FR01 服务端显式绑定决定模式；错误配置不回退，普通模板继续当前内核。
- FR02 Finder 复用 Word 结构和当前合法本体，隔离规则，无模型/PDE/报告/事实写入。
- FR03 每用户/模板/源一份当前执行头和最新缓存；显式开始/重跑，GET 不派发。
- FR04 节点和属性分别定位；外部/计算来源独立，歧义不得伪造锚点。
- FR05 两页面共享 Finder 结果；V1 可展示，V2 可选择 Finder；历史报告保持可读。
- FR06 鉴权、幂等、过期执行拒绝；内部作业不进入普通源/候选/报告入口。
- FR07 报告模板详情页可由高级分析师切换关系图谱识别引擎，V1/V2 均适用；按当前修订保存，报告中心共享该配置。Finder 仅可选择支持模板根类的内置配置，静态演示模板不适用。
- FR08 切换不启动、取消或清除识别任务/结果；已有任务保持原执行域，展示使用当前引擎。并发修改须提示刷新；模板结构、发布状态及新修订默认分流不因切换改变。

2026-09-14 补充澄清：页面显式保存的引擎优先于部署绑定；未在页面配置的修订仍读取原绑定文件。保存为本体指引可覆盖已有 Finder 文件绑定。新修订不继承旧修订的引擎设置，仍须单独选择。

展示命名：按用户要求，原“旧版 Finder”引擎在模板页、报告中心、识别操作及提示中统一称为“本体指引1.0”；内部 `finder_legacy`、profile ID 和接口路径保持原契约。

验收分别覆盖路由、真实 Finder 输出、属性出处、无模型/事实写入、并发和 owner、源变更、页面切换及正常路径回归。工程通过不代表真实演示原件达标。

## 已批准的 Slot 与报告兼容扩展 — 2026-09-14

用户已同意并要求实施[兼容方案](../../docs/Slot语义与本体指引1.0报告结果兼容方案-20260914.md)。FR02 的报告限制保留在 Finder 识别 worker 内；新增只读消费当前 Finder 结果的演示报告入口，复用 V2 报告执行，不进行事实提交。

- FR09 内容项保留 Slot 的来源范围、字段、必填/缺失、重复和行文语义；AI 分析生成可执行建议，保留人工编辑。
- FR10 指定模板可从本体指引1.0当前结果及明确配置的本地 Mock 解析输入，真实缺口、来源和对象身份不丢失。
- FR11 两种引擎共用报告模板与生成操作，切换不清空语义、不隐式识别；Finder/Mock 输出为演示草稿，服务端拒绝正式签署发布。
- FR12 预览、AI 引用式行文及 DOCX 使用同一次解析输入；空绑定和缺失配置不能显示为齐备。

澄清：旧 Prompt 转为授权引用和有限措辞，不保证旧自由文本逐字一致；原文风险等级不作为本次计算结果。无数据库新表、无新依赖、无本体修改、无新增审批流程。

## Section 自定义行文恢复 — 2026-09-15

- FR13 在 Section 内生成/编辑 Prompt、选择已绑定输入、预览 AI 行文，保存后参与报告生成。
- FR14 自定义 AI 草稿允许按作者 Prompt 组织措辞，继续保留输入引用、真实缺口和服务端草稿用途限制。该需求扩展此前仅固定措辞的行文范围。
- FR15 Section 配置转换为既有 V2 输出，已有内容项独立 Prompt 保留，迟到建议不覆盖人工编辑。

实施及验收口径见 [section-narrative.md](section-narrative.md)。

## 指定 HRS-1597 报告的模板与 PDE 审核 — 2026-09-15

用户明确限定为 `HRS-1597原料临床备样生产信息表_--脱敏-原 - PDE-冲突.docx`：

- FR16 报告中心模板上下文只显示并选中“风险评估文档V1版”的最新修订，沿用同名模板按创建时间取最新的规则；旧深链也指向最新修订。其他文档保持原选择行为。
- FR17 关系图谱面板恢复 `b6f9bd4e55822e20dce0a2ef0fa8f6d9fdd472c1` 的三栏布局、可调图谱宽度、标题行“重新识别”按钮、关系分组及 PDE 冲突内联样式，保留当前精确原文定位。
- FR18 显式重新识别时按同一原文节点的毒理参数和因子比较 PDE，展示推导/原文、公式与 F1–F5；输入不足明确待复核，不补造参数或阴性结论。仅此 Finder 展示范围恢复确定性比较，不调用模型或提交事实。
- FR19 高级分析师可采纳推导、采纳原文或标为待复核，沿用已有人工决策存储和版本冲突提示。审核须对应当前用户、模板、原件及已完成执行；重新识别后待复核，旧决定不自动套用。GET、刷新和模板选择不启动识别或保存审核。

本节是 FR02 对该指定文档 PDE 展示及人工审核的明确扩展；通用 Word 退役入口和其他 Finder 文档保持原边界，不恢复旧的隐式计算/执行管线。

实现、测试和加载结果见 [HRS-1597 验证记录](hrs1597-review-validation.md)。
