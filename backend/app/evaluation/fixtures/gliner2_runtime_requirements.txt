# Isolated CMC GLiNER2.5 experiment; gliner2 package 2.0.0 provides boundary architecture.
# Do not install into the live backend environment.
# Inherit the existing container's torch==2.7.1+cu126 and its CUDA libraries.
# The backend uv.lock and application NER default remain unchanged.
gliner2[local]==2.0.0
transformers==4.57.6
huggingface_hub==0.36.2
peft==0.21.0
accelerate==1.15.0
tokenizers==0.22.2
safetensors==0.8.0
requests==2.34.2
urllib3==2.8.0
psutil==7.2.2
protobuf==6.33.5
