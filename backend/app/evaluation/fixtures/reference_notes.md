# CMCReport 独立源文参考集说明

参考文件：`cmc_upload_23c872fb_reference.json`。源文档 ID 为 `upload-23c872fb-3ab1-41de-a705-dd4b162dfa09`，SHA-256 为 `e94822808601e73f4ba4c715c664812d4a01e42f0e4e84a6b08a3653b915daf7`。

本参考由独立助手阅读原 Word 文档的全部 447 个 IR 证据单元（5,398 字符）以及项目本体后建立；未阅读抽取预测、候选图谱、模型输出或试验结果。它是 `assistant_silver`，没有经过人工或 CMC 专家复核，不应称为人工金标准或客观全篇准确率。

参考第 3 版含 14 个实体、31 个属性、13 条关系，共 58 个肯定参考项，以及 15 条禁止断言检查。另有两项原文 NOAEL 数值观察因缺少单位而不计分。覆盖简介中的临床备样计划、产品身份和选择性基本性质、四台身份明确的设备、两条分开的动物毒理记录、三种有依据的降解途径。六个 `scopes` 仅声明所列证据单元内、所列类别/谓词的局部穷尽性；范围外内容仍未标注，不能自动当作错误预测。

标注约定如下：

- 实体 ID 仅为参考集内部稳定标识。`aliases` 取自原文；计划、大鼠数据、犬数据这三个匿名信息记录带 `identity_mode: source_record`，使用类别加唯一记录身份提示的原文证据对齐，允许不同的合成显示名。其他实体使用严格别名、类别及证据对齐。文档根带 `is_document_root: true`，按源文件哈希与类型对齐，是外部提供的元数据，应单独计数而非当作模型识别成功。
- 证据偏移为原始 IR 单元内 Unicode 字符的 `[start, end)`；`quote` 必须等于对应切片。多个证据位置为允许的替代依据，而非要求预测同时重复引用所有位置。表格的主体与值归属结合原文行列检查。
- 所有计分和禁止属性的 `literal` 使用生产 `backend/app/schemas/evidence.py` 中的 `LiteralValue` 契约。数值 `kind` 为 `number`，`normalized_value` 使用十进制字符串，`datatype_iri` 为 `xsd:decimal`；布尔属性明确使用 JSON `false` 和 `xsd:boolean`；计划月份为 `date`/`xsd:gYearMonth`，文本为 `text`/`xsd:string`。`canonical_unit` 按本体注释分别为 `kg`、`mg/day` 或 `null`，且必须存在源文单位支持。`assertion_status: affirmed` 表示该值被原文支持，不意味着否定值被改写成肯定值。
- 简介中的 2026 年 6 月、3.8–6.6 kg 均为计划；不能替换成产出量 3.0–6.0 kg，亦不能由计划反推已经完成生产。设备关系按本体 `usesEquipment` 的“所需设备”语义标注，并记录 `specified_requirement`。
- 原文 `PF64216或PF64616` 为二选一，未将其标成两台实际使用的设备。条件性重结晶不构成实际已经发生重结晶的执行事实。
- 原文产品结构节的分子量 500.60 与设备主残留物列的 500.18 冲突。只对产品结构节的 500.60 做局部参考；未由常识、计算或模型决定哪个值正确。
- 大鼠和犬的共线评估数据保持独立：NOAEL 原表数值为 200/30，PDE 为 100/7.5 mg/天，来源编号分别为 19191-25030-NG/19191-25031-NG。NOAEL 原表没有单位，不能按本体猜测为 mg/kg/天；两项数值移至 `unscored_observations`，单位保持 `null`，从主准确性评测的分子、分母和穷尽范围排除，避免正确弃答被计为漏检。PDE 的单位由原表表头提供，继续正常计分。
- 毒性信息表的 `×` 表示无相应毒性，`—` 表示相关研究数据不充分。研究不足不能变成“否”。`CAS：N/A` 也不能成为一个真实 CAS 标识。
- 降解段支持氧化、光照和高温条件下的降解；该句对酸碱说“比较稳定”，不能仅因出现“酸”“碱”字样就肯定抽取酸/碱降解。此禁止项仅约束本句，不声称物质在所有条件下绝不会酸碱降解。
- `DrugProduct` 的类注释偏向制剂，但项目 `CMCReport → describes` 的谓词注释明确包含 drug substance；按该 CMC 数据模型标注 HRS-1597 产品层记录，不强制补造独立 API 节点。

应报告参考覆盖范围内的召回率、有限范围精确率、禁止断言命中率、证据定位和主体归属等指标；不能将 58 个参考项当成整个文档的完整图谱。参考本身的领域正确性和匿名节点对齐规则仍应经人工复核。

第 2 版修订发生于任何预测和模型运行之前，仅纠正 NOAEL 缺失单位的标注政策；没有查看预测结果，也未基于方案表现调整期望答案。

第 3 版同样在任何模型运行前修正序列化表示，将近似草案中的 `kind: decimal` 改成生产使用的 `kind: number` 并声明准确的 XSD 类型。31 个肯定属性和 8 个禁止属性，共 39 个 `literal` 均经过生产 `LiteralValue.model_validate` 验证，同时逐项核对本体 `rdfs:range` 及 `canonicalUnit`。语义事实、计分范围和数量不变。

评测时必须在模型运行之前冻结该文件并保存其 SHA-256；参考内容不得提供给摘要生成、标签规划或抽取模型。后续若复核修订参考，必须升版并对全部比较组统一重评，不得只修正对某一方案不利的项目。
