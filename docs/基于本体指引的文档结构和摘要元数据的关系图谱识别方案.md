# 基于本体指引的文档结构和摘要元数据的关系图谱识别方案

状态：重构设计稿，尚未实施。编制日期：2026-09-08。当前代码基线：工作区提交 `89766a7`；它不等于已部署服务或历史实验的运行快照。

本文承接[原结构与摘要方案](基于文档结构和摘要元数据的关系图谱识别方案.md)，以 [CMCReport 质量优先实测报告](CMCReport质量优先图谱识别实测报告.md)及 [09 冻结实验](evaluations/cmc-product-api-staged-20260908-09/README.md)为主要证据，给出模块拆分、数据契约、执行状态机、生产接入和验收顺序。后续实施以本文的重构约束为准；历史实验和原方案保留，不改写其结果。

凡标注“拟新增”“目标接口”“验收要求”的内容，均不是当前代码已具备的能力。本文不修改本体、生产代码、冻结输入、银标或任何已保存候选，也不承诺通过重构即可保证语义准确。

## 1. 决策摘要与目标

### 1.1 核心决策

采用“**本体限定当前主体的直接菜单 → 结构和摘要定位多个候选章节 → 完整原文记录发现实体 → 独立证明当前关系 → 以已关联实体递归展开直接菜单**”的主链路。

重构不是把全文 NER 换成一次大模型问答，也不是继续增加验证提示词。重点是把原来混在候选和布尔判定中的四种问题拆开：

1. 原文中是否出现某个实体提及，以及它属于什么类型。
2. 若干提及是否指向同一个文档局部实体。
3. 某个编号究竟标识哪个对象，是否足以建立跨文档唯一身份。
4. 原文是否支持当前主体与属性值/对象之间的**指定谓词**。

章节树和摘要解决“去哪里找”；原文和本体解决“找到了什么”；验证证据包解决“为什么可以这样归属”；调度台账解决“拒绝之后还找哪里”。四者不能互相代替。

### 1.2 重构目标与非目标

| 项目 | 本次设计要求 |
|---|---|
| 主目标 | 在现有本体下，准确识别文档根关联的实体、属性、关系、极性、条件及来源，支持多章节和多跳。 |
| 验证样本 | 保留 `upload-23c872fb-3ab1-41de-a705-dd4b162dfa09` 为开发回归样本；它不再是未见测试集。 |
| CMCReport 焦点 | `CMCReport —describes→ DrugProduct —hasActiveIngredient→ ActivePharmaceuticalIngredient`，再验证已到达主体的属性。 |
| 全图能力 | 相同机制覆盖根的其他直接关系，不能把描述关系失败作为生产计划、设备等独立关系停止的理由。 |
| 性能 | 记录调用数、排队、模型与非模型成本；本轮不设提速倍数验收，不以性能预算代替质量结论。 |
| 不做 | 修改 DrugProduct 定义、增加 CMCReport→API 直连、补造组成关系、按同名强制合并、把未评分预测当正确。 |
| 范围控制 | 首先完成 Word 的生产证据链路重构；Excel、数据库和既有声明式抽取保持现有路径，除共享契约适配外不一起重写。 |

## 2. 实测给出的依据，而非预设的成功结论

### 2.1 必须继承的事实

09 使用 runner `evaluation-quality-record-graph-v5.2`、上下文 `model-context-v6-verified-identity`、原子引用 v4，runtime SHA-256 为 `bd26bde7b852cb1d5baebaabfcb3a8f06ce661dc921a954ed46b09a367614af0`。原文、本体、IR、schema、摘要及既有参考没有改变。

| 实测现象 | 设计推论 | 不能推出的结论 |
|---|---|---|
| call 20 拒绝 describes，后续其他记录在 call 22—25 建立第一跳。 | 保留多章节召回；拒绝是候选级/断言级事件，不是文档级否定。 | 首次拒绝理由准确，或 DrugProduct 全局身份已成立。 |
| API 首次归属被拒绝后，其他 API 记录继续执行；属性拒绝后也继续处理其他属性/关系。 | 拒绝后的续检机制应保留，并统一到所有在范围内的谓词。 | 所有记录已经尝试，或真实文档已进入第二阶段。 |
| call 31/48 已看到“来源”表头，仍将两个试验来源号判作 API 唯一标识。 | 身份角色必须独立验证；不能只修复 identity_supported 的传递。 | 删除身份状态、隐藏表头或按同名合并可以解决问题。 |
| call 50 合并名称+表头无法定位唯一行；51 加第一行来源格后可定位 row 1。 | 表格记录定位与实体标识、关系语义必须分层。 | 来源格帮助定位，就证明该格是 API 标识。 |
| call 52 引用了产品代码和 API 名称/表头，用报告/describes 关联推导组成并通过。 | 双端引用检查之外，需要指定谓词的原文桥接证明。 | 存在完整图路径就代表关系正确。 |
| 适应症 reference 返回的主体 ID 是 Product，但解释转为核验 CMCReport。 | 每次验证绑定明确的目标四元组，根上下文与当前主体角色分开。 | 程序把 subject 改成了根；或删除 effective_class 一定能修复。 |
| appearance 保存为“类白色到白色粉末”，同节另有“口服片剂”。 | 连续字段块的局部归属与原料药/成品层级分别验证。 | 同节字段一律属于同一个成品制剂实例。 |
| CAS 的 N/A 未保存；binding=false 无理由，之后 reference 仍被调用并拒绝。 | 缺失值、语义拒绝、缺失理由分别记录；前置明确拒绝后可停止该条断言的后续校验。 | 系统已经正确实现了 CAS 缺失值策略。 |

09 共 52 次模型调用、24 次任务尝试、20 个谓词—记录观察；最终结构根图为 3 个节点、2 条边、1 个属性，但完整组成链路的原文语义见证未成立。队列仍有 242 项：73 个 describes、82 个 hasActiveIngredient、86 个属性及 1 个 API 展开任务。两条焦点关系的真实第二阶段执行数均为 0。

固定银标排除给定根后有 57 项。validated 为 TP=2、FP=0、FN=55、unscored=4，命中是 Product 和 appearance；describes 因保存来源不同于参考限定来源而未匹配。API 和 hasActiveIngredient 没有参考正例，参考认可根路径为 0/13。这个来源匹配口径、程序图连通性和独立语义准确性必须分开报告。

### 2.2 已验证能力与尚未验证能力

已具备、应复用：统一 IR、原文回放、物理表格单元和合并覆盖、原子引用权限、局部本体菜单原型、多章节分期原型、版本依赖检查、候选原始审计及拒绝续检合成测试。

尚未完成：可靠的谓词桥接验证、身份角色证明、物理 mention 与实体身份分离、全菜单统一补充召回、生产版本化落库适配、生产审核门禁适配，以及独立金标上的完整文档质量验收。375 项工作区回归通过只能证明已有实现契约，不能代替这些验收。

## 3. 当前版本的真实接入点与差距

### 3.1 生产与评测不是同一条调度路径

| 当前文件/入口 | 已有职责 | 重构处理 |
|---|---|---|
| [api/document_analysis.py](../backend/app/api/document_analysis.py) 的 `_analyze_docx` | 无状态结构预览与章节摘要；不执行关系图谱识别。 | 保留展示用途；如复用其摘要，必须转换成来源/版本匹配的 MetadataSnapshot，不能假定与识别作业自动共享。 |
| [api/extraction.py](../backend/app/api/extraction.py) 的 `_compute_annotation` | Word 解析、预览、创建 configured_generic_runner、抽取、之后生成/延迟生成摘要、进度与缓存。 | 拆出识别应用服务；预览继续只读，元数据准备先于需要它的检索计划。 |
| [local_semantic_model.py](../backend/app/services/extraction/local_semantic_model.py) 的 `configured_generic_runner` | 本地模型、精确 tokenizer、预算、GenericExtractionRunner 配置。 | 保留模型设施；以显式 runner 工厂选择新旧编排，不把调度策略塞入模型客户端。 |
| [extraction_tasks.py](../backend/app/services/extraction/extraction_tasks.py) | 任务模型、召回、类型/绑定/reference、规范化、校验及通用调度。 | 先抽出可复用任务执行器与协议，再替换调度；不继续扩大单个 `_run`。 |
| 同文件的通用 `_run` | 有给定根时，先按 max_hops 收集本体可达类及其子类，再形成类型菜单并遍历原文窗口。 | 替换为实例前沿驱动，不预收集多跳类型。当前并非毫无限制地遍历全本体，但仍存在多跳类×原文窗口的候选膨胀。 |
| [quality_guided_variant.py](../backend/app/evaluation/quality_guided_variant.py) | 实验性路由、分期、递归、归并、调和、checkpoint。 | 拆成生产服务中的小模块，评测调用相同核心；不让生产 import `app.evaluation`。 |
| [record_retrieval.py](../backend/app/evaluation/record_retrieval.py)、[staged_retrieval.py](../backend/app/evaluation/staged_retrieval.py) | 记录索引、范围授权区分、焦点关系两阶段。 | 提取为记录与检索模块；统一到所有在范围内的属性/关系。 |
| [hierarchical_context.py](../backend/app/services/extraction/hierarchical_context.py)、[verification_context.py](../backend/app/services/extraction/verification_context.py) | 身份状态投影、上下文与验证类菜单。 | 收口到一个 ContextAssembler；新类型/身份/关系验证不各自拼接自由字典。 |
| [table_records.py](../backend/app/services/extraction/table_records.py) | 物理 cell、多段、列头、合并行列、逻辑记录映射。 | 保留确定性定位，新增独立语义角色层，不能把坐标推导当语义证明。 |
| [instance_registry.py](../backend/app/evaluation/instance_registry.py)、[quality_reconciliation.py](../backend/app/evaluation/quality_reconciliation.py) | 基于已验证身份归并、版本重写、依赖撤销。 | 拆为 MentionRegistry、EntityResolver、DependencyIndex；复用正确部分并扩充证明契约。 |
| [candidate_store.py](../backend/app/services/extraction/candidate_store.py) | 作业级候选 ID、不可变版本、审核、持久化。 | 增加批量发布映射和显式审核策略；不能直接接收实验图就视为兼容。 |
| [annotation_execution.py](../backend/app/services/extraction/annotation_execution.py)、[checkpoint_journal.py](../backend/app/services/extraction/checkpoint_journal.py) | 执行所有权、暂停恢复、文件快照/日志。 | 保留租约与 fencing，增加版本化 runner checkpoint/进度适配。 |
| [evidence-graph.ts](../frontend/src/lib/evidence-graph.ts) | 候选展示树、连通分组及发布筛选。 | 候选连通视图保留；新增服务端判定的有效路径/待验证/已审核视图，不用展示连通性计算质量。 |

Word 的 `/analysis` 入口不是 [pipeline.py](../backend/app/services/extraction/pipeline.py) 的旧正文 NER 分支。本次不恢复已退役的业务抽取器，也不将旧分支改名后接回生产。

### 3.2 三个生产集成阻断项

1. **摘要时序**：当前 `_compute_annotation` 先抽取，再生成或延迟生成摘要。新检索不能假定页面可见的摘要已经进入该次抽取；必须显式准备并冻结 MetadataSnapshot。
2. **候选版本与审核**：`persist_validated` 会归一化新候选为 revision 1，但不同步映射引用中的 revision；对已存在 ID 直接跳过，以保护既有人工结果。09 的 Product@2 直接接入可能造成端点失配，后续归并/撤销也可能未发布。`automatically_review` 还会把通过校验的文档候选默认改为 confirmed。实验中的全部 pending 不是生产默认行为。
3. **进度与显示**：生产 `_checkpoint_counts` 读取 completed/failures，而 quality checkpoint 使用 task_events/queue/coverage/staged_plans。直接替换会错报进度。前端 connected/roots 包含未通过或被拒绝关系，只是候选展示，不是严格有效路径。

这三项未解决前，新 runner 只能隔离评测或影子运行，不得作为生产默认实现。

## 4. 不可破坏的重构约束

为测试与审阅分配固定编号：

| 编号 | 约束 |
|---|---|
| I01 | 本体限定菜单及类型，不由模型新增 IRI、改 domain/range 或放宽类定义。 |
| I02 | 摘要、标题排序、候选置信度、已有图路径不是新增事实来源。原文标题可作为明示的上下文，不自动当实体名称。 |
| I03 | 检索范围不等于主体归属授权；授权必须绑定主体、谓词、范围、证据及版本，不能跨谓词泛化。 |
| I04 | 类型成立不要求全局唯一编号；类型、局部共指、全局身份分别判定。 |
| I05 | 物理 mention 去重不等于实体合并；同名、同项目、不同试验来源号均不能单独决定实体数量。 |
| I06 | 两端分别有证据不等于关系成立；关系须有与当前谓词匹配的原文桥接。 |
| I07 | 既有父关系只能授权递归和解释路径，不能证明子关系、子属性或产品组成。 |
| I08 | 类型/关系/属性拒绝不删除其他记录；技术失败不记作原文否定。 |
| I09 | 肯定事实、否定事实、条件事实、模型拒绝、审核拒绝是不同状态。 |
| I10 | 只有当前版本、证明有效、无条件肯定且未被审核拒绝的路径可以驱动肯定递归。 |
| I11 | 原文引用沿用 ID/精确子串协议；坐标由程序计算，权限、回放与语义分别校验。 |
| I12 | 归并、拆分、审核或证据变化导致的依赖更新必须可追踪；不能静默升级旧引用或覆盖人工修改。 |
| I13 | 每个范围内谓词均有检索台账与补充召回；路由阴性不得记作已穷尽。 |
| I14 | 原始提议与全部判定不可变；新版本结果、重新评分及专家审阅不能改写历史实验。 |
| I15 | 读取页面不得触发模型或候选写入；模型调度记录、候选存储和事实提交是三个不同写入域。 |

## 5. 目标架构与模块契约

### 5.1 处理流

```text
DocumentIR ──→ RecordIndex ──────────────┐
     └──────→ MetadataSnapshot ──→ 检索排序│
OntologySnapshot ──→ LocalMenuCompiler ─┤
                                       ▼
已知文档根 → FrontierScheduler → RetrievalPlan → RecordContext
                 ▲                              │
                 │                     发现 mention / 属性提议
                 │                              ▼
                 │                   Type / Role / Identity 验证
                 │                              ▼
                 │                   PredicateEvidence 验证
                 │                              ▼
                 └─ 仅有效肯定边 ─ ProofGate / EntityResolver
                                                │
                             RunLedger + DependencyIndex
                                                │
                           GraphProjection → PublicationAdapter
```

摘要与结构只进入检索及带角色的原文上下文。模型阶段通过受控协议调用；模型没有写入注册表、授予权限、合并实体或提交事实的能力。

### 5.2 建议的代码布局

以下目录及类名是目标布局，不表示当前已有文件。实现时在 `backend/app/services/extraction/ontology_guided/` 下建立职责明确的模块；领域契约放入独立 schema 模块，避免循环导入。

| 拟新增模块 | 输入 → 输出 | 明确不负责 |
|---|---|---|
| `contracts.py` / 对应 schemas | 类型化领域记录、版本与序列化 | 模型调用、隐式状态转换 |
| `ontology_plan.py` | OntologySnapshot + SubjectRef → LocalMenu | 文档候选类型发现、全文扫描 |
| `metadata.py` | IR + 摘要版本 → MetadataSnapshot、检索提示 | 事实候选和授权 |
| `records.py` | IR → 物理/逻辑记录索引、FieldGroup、TableFrame | 按字段名直接产出事实 |
| `retrieval.py` | SubjectRef + SlotSpec + Records + Metadata → 分期计划 | 因排序低永久剔除记录 |
| `context.py` | TaskTarget + Record + EndpointEvidence → TaskContext | 删除必要反证以适配 token 上限 |
| `mentions.py` | 原文提议 → MentionRegistry、局部实体假设 | 全局实体去重 |
| `verification.py` | 当前目标和原文包 → 分阶段 Decision/Proof | 直接写候选状态或事实图 |
| `resolution.py` | Mention/IdentityProof → 可审计的局部归并/全局关联事件 | 按同名合并、拿模型 true 当唯一凭证 |
| `scheduler.py` | Frontier + RecallLedger + 决策事件 → 下一任务 | 自行猜测语义、隐式吞掉失败 |
| `ledger.py` / `dependencies.py` | 事件 → checkpoint、派生状态、依赖闭包 | 重写原始模型答复 |
| `projection.py` | 当前候选及证明/审核状态 → 各种图投影 | 从图连通性推导事实正确 |
| `publication.py` | 已验证 run 批次 → 作业候选版本映射 | 绕过既有审核和事实提交接口 |

`GenericExtractionRunner` 逐步保留为旧编排及共享执行器的兼容入口；evaluation 下的 quality runner 最终变成调用新核心的薄适配器。历史冻结 runtime 不移动、不恢复到新核心。

### 5.3 目标接口

下面是领域接口签名示意，不是可直接粘贴运行的库 API：

```text
compile_local_menu(ontology, subject_type) -> LocalMenu
build_record_index(ir) -> RecordIndex
prepare_metadata(ir, summary_snapshot | none) -> MetadataSnapshot
plan_slot(subject_ref, slot_spec, index, metadata, coverage_policy) -> RetrievalPlan
assemble_context(target, record_ref, endpoint_evidence, proof_dependencies) -> TaskContext
discover_mentions(context, allowed_range_types) -> ProposalBatch
verify_type(context, mention_ref, type_iri) -> TypeDecision
verify_identity_role(context, identity_proposal) -> IdentityDecision
verify_predicate(context, assertion_proposal) -> PredicateDecision
apply_decision(decision, expected_state_revision) -> LedgerEvents
resolve_mentions(proof_refs, expected_entity_revisions) -> ResolutionEvent
next_task(frontier, recall_ledger, dependency_index) -> Task | StopState
project_graph(run_revision, projection_policy) -> GraphProjection
publish_batch(run_revision, expected_job_revision, review_policy) -> PublicationMap
```

纯函数层可使用固定 IR 和模拟模型测试；所有有状态变更从事件接口经过版本检查。生产和评测共用这些接口，评分器不得参与任何一个接口的输入。

## 6. 数据契约：先分清对象，再编排模型

### 6.1 四层实体模型

| 层次 | 拟新增对象与关键字段 | ID 与约束 |
|---|---|---|
| 物理提及 | `SourceMention`：mention_id、analysis_id、source_cell_id、source_spans、text、record_view_refs | ID 来自文档/解析身份和物理来源区间，不含当前逻辑行、检索阶段或模型身份判断。同一合并 cell 被两行观察时只增加观察引用。 |
| 文档局部指称 | `LocalReferent`：referent_id、revision、mention_refs、coreference_proof_refs、alternative_refs | 名称/代码等不同提及只有经局部共指证明后归入同一指称；允许没有全局编号。共享 mention 不自动证明两项研究使用完全相同的物料实例。 |
| 本体实体解释 | `EntityInterpretation`：entity_id、revision、referent_ref、class_iri、type_decision_ref、qualifiers、level_evidence_refs | Product/API/批次等解释可竞争，不能因任务要求 DrugProduct 就赋予该类型；类型未决的解释不驱动肯定递归。 |
| 跨文档身份 | `IdentityClaim`：identity_claim_id、entity_ref、key_predicate、value_span、field_role_refs、identified_role、namespace、uniqueness_scope、qualifiers、decision_ref | 本体允许的身份属性只是必要条件；编号角色、命名空间和唯一性范围必须有依据。没有这些依据不建立全局链接。 |

局部实体句柄与全局实例 IRI 分开保存，建议增加 `identity_origin=document_local/verified_external/verified_key`。当前 `urn:evidence:resolved:` 的存在不能被解释成跨文档身份成立；必须检查其实际生成规则及适用域。

`qualifiers` 表示原文明示的时间、剂型、规格、批次等适用限定，值仍需来源；不是模型可自由发明的新本体属性。无法通过当前本体表示的事实保留为 `unmodelled_observation`，不偷偷映射到“最接近”的错误类或属性。

### 6.2 统一验证目标

新增 `VerificationTarget`，由程序构造并冻结：

```text
target_id = hash(
  run_fingerprint, claim_id@revision, check_kind,
  subject_ref@revision, predicate_iri, object_ref@revision | literal_hash,
  assertion_polarity, applicability, source_scope_hash, context_hash
)

VerificationTarget:
  target_id, claim_ref, task_id, check_kind
  document_context: {document_hash, document_class_iri, root_ref}
  subject_ref, subject_class_iri, predicate_spec_ref
  object_ref | literal_ref
  ontology_hash, source_scope_hash, context_hash
```

模型响应使用该请求允许的 `target_id`，程序以完整服务端目标检查返回的角色与端点。没有可自由替换的目标 class、predicate、revision。文档类型保留在 document_context，不以单个 effective_class 同时代表文档和当前主体。

目标签名只能防止目标串用，不能检查理由是否真的在讨论正确对象；这一点仍由角色证据、谓词证明和独立语义审阅承担。

### 6.3 判定不是一个总布尔值

新增通用 `SemanticDecision`：

| 字段 | 要求 |
|---|---|
| `decision_id / target_id / check_kind` | 指向唯一检查目标；check_kind 区分 type、field_role、local_coreference、global_identity、predicate_entailment、applicability。 |
| `verdict` | `supported / unsupported / undetermined / not_checked`。技术失败单独放执行事件，不伪装为 unsupported。 |
| `reason_code / reason / reason_status` | 新请求的支持、拒绝和不确定均要求理由及代码；not_checked 由程序写前置条件原因。旧 trace 缺理由仅标 missing。 |
| `support_refs / counterevidence_refs` | 可回放原文引用；无反证列表不能解释成“已检查全文且无反证”。 |
| `searched_context_refs` | 明确本次实际看到的记录及范围；不可引用当次未提供的远处材料解释模型判定。 |
| `verifier_version / model_identity / attempt_id` | 可追踪模型输出、重试与版本；后续重验追加新判定，不覆盖旧判定。 |

新增 `VerificationBundle` 保存定位检查、类型/角色/身份/谓词检查、适用性检查、依赖检查。分清 `structural_valid`、`model_supported`、`policy_eligible`、`independent_review` 四层，不能从其中一层自动填充其他层。

模型拒答另记 `model_refusal`，其语义结果为 undetermined；禁止用空数组冒充已经逐项验证过的 unsupported。

### 6.4 关系与属性证明

`PredicateEvidence` 的最小字段：

```text
proof_id, proof_revision, target_id, predicate_iri
subject_role_refs
object_role_refs | value_role_refs
predicate_support_refs
bridge_kind
bridge_steps[]: {step_kind, input_refs, output_role, source_refs, decision_ref}
competing_owner_refs, counterevidence_refs
applicability_refs, dependency_refs, verdict, proof_policy_version
```

其中 predicate_support_refs 必须提供支持**当前谓词**的原文；不能仅重复主体名称、对象名称和父边来源。允许结构化字段组/表格映射与跨段指代作为证明，不机械要求原文出现谓词中文标签。

初始桥接类别：`explicit_assertion`、`owned_field_group`、`role_mapped_table`、`resolved_reference_chain`、`document_subject_description`。`same_document`、`same_name`、`adjacent_only`、`path_reachability` 只能作为检索/解释线索，不是可接受的桥接类别。派生推理若将来接入，须有已审核规则及独立 provenance，不能伪装为原文抽取。

### 6.5 原文角色与值存在状态

增加 `FieldRoleClaim`：`span_refs、header_refs、record_view_ref、owner_ref/owner_hypothesis、role_kind、ontology_slot_ref、verdict`。role_kind 可表达名称、属性值、项目编号、试验来源、批次标识、文档编号、组成成员、条件等**抽取角色**；它不是新增 OWL 类。角色不明时保留 unknown，不按编号外观判定身份。

增加 `ValueObservation`：`slot_ref、raw_text、source_refs、presence、interpretation、normalizer_version`。presence 至少区分 present、placeholder、not_mentioned；placeholder 的解释区分 unknown、not_applicable、unspecified。N/A 无补充语境时记 placeholder/unspecified，不擅自解释为未知或不适用，也不作为 CAS 事实值。

“否”可以是有效布尔值，不能被缺失值过滤器删除；原文没有提到、原文否定和模型拒绝也不能合并。

## 7. 本体直接菜单与元数据提示

### 7.1 LocalMenuCompiler 的确定性算法

先增强只读 OntologySnapshot：在当前扁平 schema 之外保留 declared_by、原 domain/range 定义、继承路径、同 IRI 的各项约束及定义来源 hash。当前 `semantic_schema_from_engine` 按属性 IRI 覆盖继承定义，单独消费该投影不足以证明约束未放宽。R2 须从引擎只读定义构建可追溯快照，规范“并列 range 声明、显式 union、继承限制”等不同语义；解析器不支持的表达式标 constraint_unresolved，不能把它们一律当可选类型并集。该变化是抽取快照契约增强，不是修改本体。

1. 固定 OntologySnapshot，读取当前主体实际通过的 class_iri。
2. 收集该类及本体允许继承的属性/关系，保留来源类与约束。这里的“直接”指**当前主体的一个谓词步骤**，不排除合法的类继承属性。
3. 数据属性产生 SlotSpec；对象关系产生 EdgeSpec，并只展开其正式 range 及合法子类闭包作为对象候选菜单。
4. 为本次关系的合法 range 类读取其名称、定义、身份属性和一层直接字段，作为检索线索；这不是预先创建二跳实体任务。
5. 关系通过之后，才为具体对象实例编译它自己的直接菜单。多跳类集合不得提前进入全文实体召回。
6. 相同谓词的继承约束须按增强快照保留的正式本体语义合并，不能用最宽 range 覆盖更窄约束。无法解析的约束显式诊断，不默认允许。

domain/range 是约束，不是存在性证据。不能因为 DrugProduct 有 hasActiveIngredient 就认为报告里必有该边；不能把逆关系、未声明属性或非当前类菜单偷偷加入任务。合法子类太多时按确定性小批处理，台账保留剩余类型，不依赖首批命中裁掉后续类型。

类型验证可附带少量必要的相邻类/竞争角色定义，用于区分实体与容器、试验、API/制剂；这属于 contrast_context，不是扩充 allowed_output_types。验证上下文若出现一个非当前 range 的正确解释，只能保留类型分歧/观察，不能绕过局部菜单直接产出该边的对象。

给定根类型是任务元数据，不计为抽取成功；根类型缺失时先进入独立文档分类/用户确认流程，不能回退为从全文生成全部候选类。

### 7.2 CMCReport 示例

09 冻结 schema 的 CMCReport 菜单有 12 条关系、0 条数据属性；实现中必须动态编译，不把这个数量或全部列表硬编码。

| 当前主体 | 当前谓词 | 对象/值菜单 | 检索线索的边界 |
|---|---|---|---|
| CMCReport | `https://ontology.pharma-gmp.cn/slpra/drug-development/describes` | 正式 range 为 `https://ontology.pharma-gmp.cn/slpra/drug/DrugProduct` 及合法子类 | 产品概况、项目名称、剂型等帮助排序；API 不作为该边的直接对象。 |
| CMCReport | `https://ontology.pharma-gmp.cn/slpra/drug-development/hasSynthesisRoute` | 从当前本体读取 range | 合成路线章节是候选范围，章节标题本身不等于路线实例。 |
| 已关联 DrugProduct | `https://ontology.pharma-gmp.cn/slpra/drug/hasActiveIngredient` | `https://ontology.pharma-gmp.cn/slpra/drug/ActivePharmaceuticalIngredient` | API 表头支持寻找 API；仍须独立证明其与具体产品的组成关系。 |
| 已关联 DrugProduct | appearance、dosageForm 等本体数据属性 | 对应值类型、单位、条件 | 不因词形符合属性就跳过主体和制剂/API 层级核验。 |

原文中的“口服片剂”可支持产品解释，毒理表 API 列可支持 API 类型；二者不能仅凭 HRS-1597 同名就构成组成边。也不得为强行成功而将 API 解释成 finished dosage form。

### 7.3 MetadataSnapshot 与潜在关系—实体标签

元数据最小契约：`snapshot_id、analysis_id、document_hash、structure_hash、summary_version、summary_model_identity、generation_source、source_record_refs、node_summaries、dependency_hash`。generation_source 区分模型摘要、摘录回退、无摘要。所有摘要只关联有效的原始节点/记录，不生成 fact-eligible 来源 ID。

检索标签采用 `RetrievalHint`，而非 Candidate：

```text
hint_id, metadata_snapshot_id, node_id | record_ids
subject_type_iri, predicate_iri, object_type_iris
hint_kind = relation_target | entity_role | property_value
support_origin = heading | summary | source_label
source_record_refs, score_components, policy_version
authority = retrieval_only
```

标签只能从 LocalMenu 及当前关系的合法 range 中选择；模型输出未知标签立即拒绝。标签表示“这里值得找某关系/类型”，不能授予主体归属、确认实体类型或触发全局归并。

先采用可复现的确定性排序：当前谓词/合法 range 的原文标签、列头、章节标题、节点摘要分项计分。保持分量可审计，首期不训练一个不可解释的总分分类器。后续语义重排仅改变顺序，不改变必保留的补充队列。

GLiNER 是可选的原文 mention 召回器：动态标签由本次小菜单映射而来，输出必须回到当前原文 span；它不判定实体全局身份、属性归属或 hasActiveIngredient。缺失或未命中时仍有主召回路径。新旧对照锁定相同权重/阈值；不在本次重构中更换模型并将收益混算为架构收益。

### 7.4 摘要准备与稳定性

正式识别入口显式准备元数据，模式为 `cached_summary / generate_summary / structure_only`，选择进入 run_fingerprint。页面 GET 继续只做预览，不能因此阻塞或触发摘要生成。

可用且 hash 匹配的摘要直接复用；缺失时由显式后台识别任务生成，或记录为 structure_only。降级不能标作摘要模型成功。摘要在该次 run 内冻结；晚到的新摘要只能用于新计划版本/新 run，不能悄悄改变正在执行的排序、缓存或完成度。摘要成本单列，不藏进抽取耗时，也不能算作正文事实。

## 8. 记录检索与完整验证上下文

### 8.1 物理来源、记录视图和语义对象分开

保留 SourceRecord 的段落/逻辑表格行粒度，增加两个辅助对象：

- `FieldGroup`：同章节连续、带标签的字段块；保留每一段的来源和候选 owner，表格/标题/章节边界阻断无条件扩展。
- `RecordView`：物理 cell/段落在某逻辑行的视图，保留 column_ids、logical_row_ids、header_refs、rowspan/colspan、单位、图例、父表及相关注释。

FieldGroup 只是局部共指和属性归属的候选结构，不预先声明所有字段具有同一实体层级。RecordView 不生成新 mention，也不按试验行数生成 API 实例。嵌套表父容器只提供上下文，不能把父表文字自动变成子记录的新事实目标。

### 8.2 每个主体—谓词统一两阶段

计划键为 `subject@revision + predicate + ontology_hash + source/metadata_snapshot + retrieval_policy`。

第一阶段按高相关章节排序，默认沿用最多三个章节的实验初值；各章节轮转。第二阶段保留该次文档范围内的所有其余非标题记录，包括零匹配、路由阴性和其他章节。该机制同时用于关系与属性，不能只覆盖 focus_path。

计划满足：两阶段不重叠、并集等于范围内全部目标记录、没有记录因一次候选拒绝被移除。表头和标题可以是验证来源，但不作为单独的实体召回目标；如果原文确实在标题中陈述事实，应由明确的标题事实政策另行支持，不能偷偷放开全体标题。

提供 `focus_path` 与 `document_graph` 两种 scope：前者仅用于焦点验证，其他根谓词标 `out_of_scope`；后者全部根直接谓词都进入计划。所谓“完整两阶段”只指该运行范围内的调度覆盖，不保证已经发现文档所有真实实体。

### 8.3 范围授权必须针对断言

原文记录一旦被检索到，就可用于发现当前合法类型的 mention；这不等于它的事实可以归属于当前主体。将以下范围分开：

1. `discovery_ranges`：可发现 mention/值的原文。
2. `binding_ranges`：可解释身份、指代、单位、条件和桥接的原文。
3. `authorized_claim_ranges`：已经为当前主体及特定谓词/角色获得证明的范围。

跨记录检索先产生候选，逐条通过谓词归属检查后才产生授权。产品代码被确认，只能支持相应的身份或共指用途，不能因此授权“同份文档中所有属性都属于该 Product”。

### 8.4 TaskContext 的必需内容

上下文必须保留：当前目标、主体/对象的原文构造来源、当前完整逻辑记录、相关表头和合并映射、连续字段块、已知竞争主体、原文明示的否定/条件/反证、可绑定父容器、根与祖先路径的独立角色。

调用内来源别名 c0/e0 等仅在该请求有效，日志保存局部到全局映射。同一原始区间去重时合并其角色和权限，不因去重丢掉反证或把 binding-only 升成 fact-eligible。候选的历史模型理由可以保留在审计，不作为新的可靠原文传给下一验证器。

引用继续使用 FactSpan/BindingSpan，wire 仅为 evidence_id 与可选精确 text。禁止模型 start/end/context；唯一子串由程序计算坐标，重复子串不任选第一处。新增原子协议从 evaluation 移入公共核心时必须保持已有回归行为。

### 8.5 大记录和上下文超预算

不能静默截掉表头、另一逻辑行的反证、竞争主体或原主体证据。先进行不损失内容的结构去重和局部菜单分批；仍无法容纳则记录 `context_budget_exceeded`，保留记录并继续其他任务。

后续若支持记录投影，投影必须满足：当前字段、完整 owner/关系角色、列头、单位、合并映射、条件与竞争 owner 的证据闭包均在；其余部分进入显式未检查台账。一个视图不能通过完整闭包检查时，不允许拆成独立小格后宣称原记录已验证。首期可不实现这种复杂投影，明确超预算比假装无事实更可靠。

## 9. 验证流水线与证据门禁

### 9.1 执行顺序

每个原文记录产生 ProposalBatch，其中每条提议独立执行：

```text
协议/引用定位
  → mention 与记录角色
  → 类型检查（实体提议）
  → 局部共指、标识角色、全局身份检查（按实际需要）
  → 属性值存在性/规范化，或关系对象选择
  → 当前主体/对象角色与当前谓词桥接检查
  → 极性、条件、适用层级及竞争归属检查
  → 当前版本与证据依赖检查
  → 保存判定；计算候选可用性；必要时递归
```

无全局身份不阻断已有充分局部实体及谓词证据的抽取。没有通过的类型解释则不能建立该类型肯定节点和肯定递归；原 mention 和竞争解释仍保留。类型未知时不得为了探索二跳而先保存一条假的第一跳。

某条提议已经明确定位失败或前置语义不成立，后续阶段记 `not_checked` 并结束该条提议，继续其他提议/记录。09 CAS 的 binding=false 后继续调用 reference 是历史行为；新执行器应避免这种会遮蔽首个拒绝原因的路径。原始和派生 reason 都保留，不能只剩最后一个异常字符串。

### 9.2 类型、标识角色和唯一性三道检查

类型检查回答原 mention 是实例、属性值、章节标题还是其他角色，并根据真实本体定义判断。缺少身份键本身不是类型否定；项目代码存在也不是 finished DrugProduct 类型成立的充分条件。

身份验证分为：

1. **标识对象角色**：字段值究竟标识产品、API、项目、试验、文档或批次；引用字段/列头以及必要的 owner 来源。
2. **局部对应关系**：该标识是否与当前实体解释相关，而非同表的其他对象。
3. **唯一性适用域**：命名空间、发行方/系统、时间/规格/批次范围及本体身份约束，是否足以支持所请求的 identity scope。

只有所请求层级的全部检查 supported，才能升级该层级身份。原文没有说明唯一性，结果是 undetermined，不可用模型置信度代替。被确认属于试验来源的编号，对“API 唯一身份”提议应给 unsupported，并保留其实际来源角色，而非删除编号。

未支持身份继续只显示验证状态，不在下游主体、共享候选、路由和缓存中作为可靠 key。初次身份验证必须看得到待验证键及其来源。这保留 v6 已修复的信任传递，并新增“错误 true 也要经过角色和唯一性门禁”的要求。

### 9.3 PredicatePolicyRegistry：区分描述、属性与组成

新增版本化的验证政策注册表，输入是**精确谓词 IRI 及本体约束**，输出是允许的证明类型、必需角色与禁止推论。它不修改本体，不包含 HRS-1597、项目号或文件专用抽取规则；政策须经业务审核、随实验冻结。无专门政策的谓词使用通用显式断言/明确角色表格政策，不能默认放行。

| 谓词语义 | 可接受的来源形式 | 必须拦截的替代证据 |
|---|---|---|
| 文档描述正文对象 | 已知文档根、正文真实对象、原文描述结构及匹配的描述谓词；无须正文重复报告标题或逐字出现“描述”。 | 只有标题、只在目录出现、引用其他报告的对象、与本文主体无关的背景提及。 |
| 实体数据属性 | 主体明确的显式陈述、经过 owner/层级验证的连续字段块或表格字段、可回放指代链。 | 只有数值/属性词、只有同节/同报告、字段属于 API 却挂到制剂、历史批次事实挂到当前批次。 |
| 产品含活性成分 | 明确组成陈述、主体已确定的配方/组成表、经原文消解的跨记录产品—成分关系。 | API 表头、毒理试验行、同名、项目代码、父 describes、domain/range 允许该边。 |

`document_subject_description` 是对文档描述关系的证明类别，不是文档根的无限授权；不能在 hasActiveIngredient 检查中使用。只有来源属于本报告仍不足以证明对象是本报告描述的实例，需区分主题内容、背景和他文引用。

跨记录证明至少包含“指称消解”和“当前谓词陈述”两个不同责任；可以由同一片段支持，也可以由多段组成。不强制两端名称同句出现，也不能只有若干 same_referent 步骤而没有任何 predicate_assertion 步骤。

### 9.4 结构门与语义门的分工

程序硬检查：目标签名、引用域、唯一定位、角色引用完整、允许的桥接类别、端点与依赖版本、极性/条件一致、无循环证明、自我证明或仅父路径证明。

独立语义检查：引用实际是否支持所声称的角色和谓词、来源号究竟属于什么对象、粉末是否为制剂性状、跨段指代是否成立。该检查使用原文和目标契约，不以召回模型的自由理由作为已知事实。

首期保留召回与验证的独立调用；角色、身份和谓词决定分别留档，可在不混淆目标的前提下批量请求。局部 binding 与跨记录 reference 都调用同一目标/证明契约，不再维护两套目标字段缺失的协议。跨记录情况在通用谓词验证外还必须通过指称桥接；本地字段同样不能跳过 owner/层级验证。

当模型填入 `bridge_kind=role_mapped_table` 但表格没有产品角色/组成映射时，不能仅因 schema 合法放行。硬约束只能检查证明结构，语义检查仍可能误判；关键反例、独立来源审阅和试点审核门禁是不可删除的最后保障。不能宣称新增一个 verdict 字段解决了 09 的错误 true。

### 9.5 极性、条件与层级

肯定、否定、条件原文均可成为有证据的候选；只有无条件肯定关系进入肯定递归。模型认为“未获支持”不是原文否定，不生成 negated 边。

属性的 owner/type/applicability 一起核验。例如“类白色到白色粉末”可以明确属于某局部化学物质条目，但对 finished DrugProduct 的归属仍未决；保留 ValueObservation 与拒绝/待定理由，不能为了输出把该属性移到一个当前本体不允许的类上。

冻结本体若存在定义与注释冲突，记录 `ontology_constraint_ambiguity`，遵守正式类型/range 限制，对受影响提议保守待定；其他独立谓词继续。是否修改本体是后续单独治理事项，不作为本轮继续检索的前置条件。

## 10. 实例归并、图递归与失效传播

### 10.1 归并权限表

| 情形 | 操作 |
|---|---|
| 同一物理 span 在另一逻辑行再次出现 | 去重观察，增加 record_view_refs；不自动创建或合并业务实体。 |
| 连续名称/代码字段有明确局部对应 | 可建立 local_coreference proof、归入文档局部指称；全局身份继续独立判断。 |
| 相同名称、相同项目但缺局部对应 | 保留 possible_coreference，不能合并。 |
| 来源号不同而 API 名称跨行合并 | 保留一处 mention 和多次研究观察；实体数量未决，不按来源号拆成多个 API。 |
| 可靠键在相同 namespace/scope 下相同，类型及限定兼容 | 可以建立全局关联；须同时检查冲突来源、同名多剂型/批次等反证。 |
| 明确不同对象，或标识/层级冲突 | 拒绝归并；冲突不能通过第三个模糊节点的传递闭包被消解。 |

每次合并/拆分保存 `ResolutionEvent`：输入实体/mention 精确版本、决定依据、canonical 选择、别名映射、更新后的版本、受影响断言与范围、旧值及新值。继承旧 canonical ID 的稳定性，但不以稳定 ID 掩盖意义变化。

拆分是显式重新归属，不是一对多 alias 改写。旧实体解释先失效，每条属性/关系根据自己的 proof 重新确定新端点；无法唯一归属的断言保持 invalidated/undetermined。禁止把旧属性复制到所有新节点，或默认继承给保留 canonical ID 的节点。只有重新获支持的断言才能形成新版本、恢复递归或发布。

### 10.2 统一 EligibilityPolicy

从当前多个 `_usable`、positive_eligible 和图导出判断中抽出统一政策：

```text
seed_eligible(root) =
  root 的 document_hash 与冻结 IR 相同
  AND 根类型来自给定/确认的作业分类且符合根类型政策
  AND 来源角色允许（analysis_source/default_source）
  AND 根授权未被撤销或审核拒绝

expandable(entity, incoming_path) =
  entity 的类型和局部指称足以支持当前解释
  AND incoming_path 存在当前有效的肯定证明
  AND 所有必要引用版本匹配
  AND 非条件、非否定、非冲突
  AND 相关实体/边未被审核拒绝
```

RootSeedPolicy 是唯一无入边启动例外：seed_eligible 的文档根可以展开自己的直接菜单，不伪造父边或原文抽取 proof，不计入抽取 TP。普通实体不能使用该例外，也不能通过文档标题给自己授予 root 权限。

无全局身份不是否决条件。review=pending 可在隔离识别图内参与已验证路径探索，但不因此取得生产发布/事实提交资格。

对象已识别而当前关系未通过时，保留对象候选为 unassociated；不能沿失败边将其属性归到根图。其他合法关系若在后续记录独立关联该对象，可以激活对象的直接菜单。

### 10.3 依赖是证明依赖，不只是树父指针

建立 DependencyIndex，覆盖主体解释、局部共指、身份键、谓词证明、范围授权、关系路径、审核状态及原文/元数据版本。

除显式引用依赖外，还记录 proof 的读取范围与竞争集合订阅：`record/field_group + owner_role + applicability + competitor_set_version`。同一原文中后来发现的新 owner 可能不在旧 proof 的引用列表；新解释进入订阅范围时主动重算相关结论，不要求原文 hash 变化。旧 supported、unsupported、undetermined 均可能失效或需要重验，不能只重验待定项；替代 OR proof 也须证明未受同一新竞争证据影响。

一个证明中的必要依据是 AND；同一断言的独立替代证明是 OR。父边被拒绝时，依赖它的路径授权失效；若存在另外一条完整独立有效路径，应重新计算可达性，不能机械删除全部对象事实。反过来，没有独立替代证明时不能只改引用版本让断言重新通过。

已独立证明但不再根可达的候选可保留在非根投影；必须依赖已失效实体解释的断言标 invalidated。历史肯定判断、审核拒绝与当前失效状态分别保留。

### 10.4 递归与公平调度

保留主体的直接谓词轮转和章节轮转；子分支与尚未完成根分支采用有界加权轮转，默认沿用“两次子记录/一次根记录”的实验初值。关系与属性各一次轮转，不能因庞大属性菜单阻断 API 候选，也不能因描述分支未成功阻断其他根关系。

防重键至少含 entity@revision、predicate、record、scope/proof 依赖 hash；路径作为见证单独保存。循环本体不等于可以无限递归：相同实体/谓词/证据上下文只执行一次，max_hops 与任务安全上限保留。因为 hop/task 上限未探索的前沿显式记录，不能报告全图穷尽。

## 11. 拒绝、续检、覆盖与停止状态机

### 11.1 四个独立维度

| 维度 | 拟新增状态 |
|---|---|
| `execution_state` | queued、running、finished、retryable_failure、blocked_dependency、paused |
| `semantic_outcome` | supported、unsupported、undetermined、not_checked |
| `assertion_polarity` | affirmed、negated、conditional、uncertain |
| `coverage_state` | unattempted、attempted_incomplete、examined；路由分数/阴性另列 |

工作单元为主体—谓词—记录，其下有 proposal 与 stage 子事件。一次模型调用、一次提议、一个记录、一个谓词和一个图分支分别计数，不相互替换。

### 11.2 拒绝处理转移

| 当前事件 | 当前提议 | 同一记录/谓词后续 | 全局影响 |
|---|---|---|---|
| 类型不支持 | 该类型解释 unsupported，不生成肯定实体 | 继续其他合法类型/提议和下一记录 | 不宣告没有 DrugProduct。 |
| 身份角色不支持 | 身份提议 unsupported；可保留已通过的类型/局部实体 | 继续关系/属性验证所需的局部证据 | 不用该键归并，不停止主体所有任务。 |
| 缺少组成/owner 桥接 | 断言 undetermined；已独立发现对象保留 | 有界补充上下文；随后仍继续其他记录 | 不生成肯定边，不沿该边递归。 |
| 有明确相反的原文证据 | 提议 unsupported；原文否定另走否定事实验证 | 继续其他提议/记录 | 不自动写一条否定边。 |
| 引用/记录映射错误 | 执行失败，语义阶段 not_checked | 允许协议修正或留作未完成；其他提议继续 | 不记作原文无事实。 |
| 模型拒答/输出无法解释 | 保留原始答复，undetermined 或技术失败 | 有界重试，超限仍保留未解决项 | 不无限占用整个队列。 |
| token/请求超时/模型不可用 | 技术失败、原任务未完成 | 可恢复失败队列；局部失败让出调度机会 | 整体服务不可用时安全暂停，不伪造空结果。 |
| 人工审核拒绝上游边 | validation 历史不改，审核状态更新 | 重算依赖并阻断失效任务 | 其他根谓词/独立路径继续。 |
| 第一阶段无肯定输出 | 第一阶段各记录保留实际结果 | 必须进入第二阶段 | 不能因 phase1 全拒绝而结束谓词。 |

新版本初始重试政策：同一输入的语义 unsupported 不靠重复采样“刷到通过”；undetermined 只在新证据或显式上下文扩展后重验。每条提议默认最多两次有新证据的补充验证，技术重试另由 TaskBudget 限定，配置与实际次数进入 run 身份/日志。额度耗尽标待解决，不删除其他待检索记录。

补充验证额度按稳定 `claim_lineage_id` 计，而不是易变化的 proposal_id；用 evidence_delta_hash 识别实际新证据，纯 alias、revision 或计划重建不算新证据，额度跨这些变化继承。已 supported/unsupported 的提议遇到实质新证据也由第 10.3 节触发重验；先撤销不再可靠的使用资格，额度不足时保留未决，不继续使用旧通过。默认每处理一次重验，至少让出一个未尝试记录的调度机会；没有新记录时才连续处理重验。显式追加额度需记录操作者和理由，不允许循环归并绕过限额。

claim_lineage_id 由执行器首次登记逻辑断言时签发并随内容版本保留，模型不得指定。重复提议先匹配主体 lineage、谓词、对象/值及适用限定；实体归并导致两个断言 lineage 合流时已用额度并账，实体拆分时子 lineage 继承已有消耗，不因分裂获得全新额度。无法确认重复的提议保留独立审计，同时仍受主体—谓词与整轮安全预算约束。

### 11.3 RecallLedger 与覆盖守恒

每个范围内主体—谓词保存 planned_record_ids、phase1/phase2、range_type_batches、路由结果、实际任务/调用编号、每阶段判定、剩余记录及触发重验的证据版本。

要求 `planned_records = unattempted + attempted_incomplete + examined`，三者按当前有效计划互斥。queued/running/retry_pending 是执行维度，不能再加到覆盖总数中。旧计划失效时单独归档，不混入新计划分母。

examined 表示计划的必要类型批次和提议检查均有终结记录，可以全是合法拒绝或待定；待定另计 unresolved_claims。技术失败且检查未完成为 attempted_incomplete。空召回记录 `no_candidate_observed`，不是证明世界中不存在事实。

这里的“待定”必须是模型完成目标检查后返回完整、合法的语义 undetermined；召回拒答、类型决策缺项、协议失败或技术前置失败产生的 not_checked 都是 attempted_incomplete。前置语义明确 unsupported 后按政策跳过后续检查，才属于合法终结。重试额度耗尽不会把前一类未完成提升为 examined；即使可执行队列已空，仍不能映射 completion=complete。

后续执行证据必须是更晚的不同记录实际调用/任务事件；同一条提议从 binding 到 reference 不算“继续其他记录”，同记录的兄弟提议继续也单独计数。pending 队列只能证明工作被保留。

### 11.4 正常结束与暂停

新增 RunStatus，分开：

- `execution_status`：running、paused、finished、failed。
- `coverage_status`：partial、exhausted_in_scope。
- `semantic_status`：unreviewed、unresolved、reviewed；另带 unresolved_claims/invalidated_count。
- `stop_reason`：queue_exhausted、operator_pause、observation_complete、budget_limit、service_failure 等。
- `publication_status`：shadow_only、not_requested、published_candidates、committed_facts 等。

正常语义拒绝不自动令整个执行 failed；队列耗尽不自动令语义准确或完整。兼容现 API 的 completion 时采取保守投影：只有范围内必要计划已执行、没有技术未完成或未探索上限前沿，才可投影 complete，并显示“范围内处理完成”，不显示“事实全正确”。

软暂停在原文记录/原子事件批次的安全边界保存。若租约丢失或硬取消，立即停止写入，当前记录标 attempted_incomplete；恢复时按日志确定最后一个完整事件，不能当作正常记录结束。历史 09 的观察清单暂停保持 incomplete；后续真实全图验收另建运行，不复写 09。

## 12. 运行身份、缓存和恢复

### 12.1 RunFingerprint

至少包含：document_hash、analysis/structure/parser 版本、ontology/schema hash、metadata_snapshot hash、runner/协议/验证政策/解析投影/归并政策版本、模型及 tokenizer 身份、影响生成的设置、scope/focus_path、检索政策、预算与重试设置。

模型名相同不等于同一模型身份；摘要内容相同也不能省略来源/版本。记录实际生效设置而非仅记录配置文件默认值。

### 12.2 缓存分层

| 缓存 | 最低失效依赖 |
|---|---|
| IR / RecordIndex | 原文、解析版本、结构 hash |
| MetadataSnapshot | 原文/结构、摘要模型/提示版本及实际内容 |
| LocalMenu | 本体/schema、主体类型及约束政策 |
| RetrievalPlan | 主体版本、完整菜单、记录/摘要身份、身份可信状态、检索政策 |
| ModelDecision | 完整目标、原文上下文、协议/schema、模型及生成设置 |
| Coreference/IdentityProof | 提及、角色来源、限定、竞争者和全部验证版本 |
| GraphProjection | run revision、候选/证明/审核/依赖状态、投影政策 |

晚到竞争主体、身份状态变化、角色重判或审核拒绝都可能使后续验证缓存失效。不能仅以候选文本和 key 字符串缓存。

### 12.3 CheckpointEnvelope

目标 checkpoint 包含 `checkpoint_schema_version、recognition_run_id、runner_kind、run_fingerprint、execution_token、event_seq、frontier、recall_ledger、candidate_head_refs、proof_head_refs、resolution_events、dependency_index、retry_queue、scheduler_turns、progress`。其中事件持久化是权威来源，图和统计可以重建。

保留现 AnnotationExecution 的 SQL 所有权、心跳与 fencing，所有模型阶段和发布批次检查 owner；不创建第二套不受租约保护的后台任务。文件 checkpoint 与数据库发布分别有 durable 水位，用幂等批次关联；不能宣称跨文件和数据库天然原子。

运行身份必须分成两个 ID：`recognition_run_id` 在同次识别的暂停/恢复间稳定，写入 execution options；当前 `AnnotationExecution.run_id` 在每次 claim/resume 时变化，在新协议中称 execution_token，只用于本次执行租约。恢复保留识别 ID、事件序列、证明和发布映射，绑定新的 execution_token；旧 worker 的 token 不允许继续写。新建/重跑才分配新的 recognition_run_id。

旧 generic checkpoint 由旧 runner 恢复；旧 quality 冻结运行继续使用其冻结代码。新 runner 不静默升级旧 checkpoint，不覆盖或删除旧断点；API 给出明确版本不兼容与新建运行指引。若将来提供迁移工具，必须只读旧输入、写新目录、核验所有引用和事件，不在原件原地升级。

## 13. 生产持久化、审核与页面接入

### 13.1 两个图域，不能混用 revision

识别域保留 `run_candidate_id@run_revision` 和原始证明；生产域保留 `public_candidate_id@public_revision`、审核和事实快照。新增持久的 PublicationMap：

```text
(recognition_run_id, run_candidate_id, run_revision)
    -> (job_id, public_candidate_id, public_revision, content_hash, batch_id)
```

同一个识别域 revision 2 初次落库可以对应生产域 revision 1，但**所有引用必须经同一张精确映射表转换**，并保存来源版本；不能只改主体 payload 的 revision。source-local 版本与数据库版本没有“数值必须相同”的要求，语义和引用必须一一对应。

发布流程采用两遍构造、一次事务提交：

1. 核验 run、job、owner、expected_job_revision、全部待发布证明及原文身份。
2. 确定本批实体、属性、关系的生产 ID/版本，以及与既有候选的对应关系，构建完整 PublicationMap。
3. 转换 subject、object、path_root、dependency_refs、scope.subject、共享主体引用和 binding 端点；proof 中的**原始运行目标**不改写，另记录其生产投影映射，避免篡改原始 target_signature。
4. 对转换后的整个图再次检查版本、类型、实例标识、范围、依赖闭包与来源；缺端点不得发布半条边。
5. 同事务追加候选不可变版本、审核记录/失效事件、PublicationMap 与发布水位；最后更新作业 head。失败回滚该批，重试依 batch_id/content_hash 幂等。

合并后的实体 revision 变化，不自动让关联边继续 passed；先由识别域重新核验证明依赖，再发布新的映射版本。所有同义别名、旧版本与分裂事件继续可追溯。

### 13.2 人工修改保护与新旧协议

保留当前“重跑不能覆盖人工审核/编辑”的规则，但不能用“相同 ID 一律跳过”实现所有增量更新。

- 纯算法管理、未发生人工变更的候选，可以通过 expected_revision/expected_review_version 的 CAS 追加新版本。
- 有人工修改或明确拒绝的同源实体/断言，进入 publication_conflict，保留人工 head；生成待审差异，不直接重置为 pending/confirmed。
- 更换 run ID、alias 或 canonical ID 不能绕过既有审核。发布前使用来源实体 lineage 和断言逻辑键检查对应的人审记录；无法安全判断对应关系就冲突待审。
- 已提交事实不被新识别过程原地改写；撤销/替代须经过既有事实提交与快照机制。

这里的 expected_review_version 是拟新增能力，当前只有内容 revision、review_status 等字段，纯审核状态变化不提升内容 revision。R5 明确新增候选 head 的单调 `review_version`：迁移既有行以 0 表示版本化审核的起点，不伪造历史审核次数；每次新审核/撤销在同事务递增并追加审核事件。新协议写操作同时比较 expected_revision 与 expected_review_version；旧入口保留原有状态比较和锁，但不能处理 V2 发布/审核请求。历史不可变内容版本不因增加审核计数被改写。

建议新协议为独立 `CandidateV2 + VerificationBundleRef + RunProgressV2`，由 API 明确声明 contract_version。既有 Candidate v1 严格模型不能靠加任意字段假装兼容。旧候选保留历史状态，标记 legacy_validation；不能仅补一个默认 proof 字段就升级为新政策验证通过。

后端在读入口兼容 v1/v2，写入口按 runner 和协议路由；前端先具备双版本读取和未知状态降级显示，再启用新写入。新政策不能无迁移地改变旧项目默认审核语义。

版本化 codec 的覆盖范围不止 API：必须替换 CandidateStore 的 get/list/review/resolve/依赖失效路径以及 [fact_commit.py](../backend/app/services/fact_commit.py) 中 `FactCommitService._current` 等直接按 Candidate v1 解析 payload 的入口。提交时重新执行 V2 ProofGate、审核版本和当前依赖闭包检查，不能把 V2 去掉新字段后降格，经 legacy 通道提交。旧 v1 按显式 legacy 政策处理，新 v2 按新政策处理，未知版本拒绝写入。

### 13.3 最小持久化扩展

首期隔离评测继续使用文件产物；进入生产影子阶段时增加以下记录，字段名为拟新增：

| 记录 | 关键字段/约束 | 用途 |
|---|---|---|
| `EvidenceRecognitionRun` | recognition_run_id、job_id、fingerprint、contract_version、status、event_head、artifact_manifest；主键 recognition_run_id | 保存跨暂停恢复稳定的识别身份，不承担第二套执行租约。 |
| `EvidenceRecognitionEvent` | recognition_run_id、sequence、event_key、event_type、payload_hash、payload；唯一(识别ID,sequence)、唯一(识别ID,event_key) | 追加式提议/判定/归并/失效/覆盖事件，按版本重建图。 |
| `EvidenceVerificationProof` | recognition_run_id、proof_id、proof_revision、target_id、payload_hash、payload；唯一(识别ID,proof_id,proof_revision) | 不可变证明与目标，引用失效由新事件表达。 |
| `EvidencePublicationMap` | 上述源/目标 ID+revision、batch_id、content_hash；源三元组唯一，同批内容幂等 | 映射使用稳定识别 ID；写入同时检查当前 execution_token。 |

mention、局部指称、检索计划先作为类型化事件和 checkpoint 投影存储，不为每个中间字段建业务表。沿用 [models/evidence.py](../backend/app/models/evidence.py) 的候选版本、审核、断言及快照表；迁移脚本采用新增结构，禁止重写历史候选和删除旧快照。

上述四类新记录之外，对既有 EvidenceCandidateRecord 增加 review_version，新增审核事件记录其前后版本；旧审核事件可标 pre_versioned，不回填虚构序列。所有新记录带 job/识别域校验和必要索引；不能用一个新表替代现有 AnnotationExecution 的所有权判断。

日志及原文片段按原文访问权限保管；浏览器只取得授权 job 的必要审计信息，不将全模型 prompt、业务原文或标识写入通用遥测。

### 13.4 审核和发布开关

拟新增、由服务端校验的策略：

| 模式 | 新识别运行记录 | 生产候选 | 自动审核 | 事实写入 |
|---|---|---|---|---|
| `legacy` | 原行为 | 原行为 | 保持既有政策 | 保持既有显式提交流程 |
| `shadow` | 可写隔离运行/模型调度记录 | 禁止 | 禁止 | 禁止 |
| `pilot_review` | 可写 | 仅经 PublicationAdapter 写入 | 新 runner 默认关闭，保存 pending | 仍须既有审核与显式提交 |
| `enabled` | 可写 | 经过完整证明与版本门禁 | 只有单独批准的新审核政策才允许 | 不因抽取完成自动提交 |

禁止让新候选直接继承 `automatically_review` 的宽松默认。关闭试验开关只影响新作业选择，不让在途新 checkpoint 交给旧 runner；在途运行要么按原 runner 完成/暂停，要么保留并新建旧模式运行。

### 13.5 `/analysis?tab=document` 的显示契约

保留原文章节树和快速预览；增加服务器产生的四种投影：

1. **全部候选**：包含被拒绝、未归属和条件/否定内容，便于排查。
2. **已验证肯定路径**：通过当前 ProofGate、版本及根可达闭包，不等于专家确认。
3. **审核/可发布图**：在第二项基础上叠加审核及提交政策，保留 automatic/manual 来源区别。
4. **覆盖与拒绝**：逐主体/谓词显示 phase、已尝试、未完成、待办、拒绝原因、下一条续检记录及停止原因。

关系详情同时显示“主体证据、对象证据、谓词桥接、适用条件、反证/竞争者”；不能只给两段高亮原文就显示“组成已确认”。身份标签区分文档局部句柄、候选编号、已验证标识和跨文档链接。

新增统一 RunProgress，至少有 tasks_attempted、model_calls、records_planned/examined/incomplete/unattempted、phase_counts、supported/unsupported/undetermined、pending_frontiers、stop_reason、contract_version。前端不解析 checkpoint 私有字段；旧 SSE/API 字段由明确适配器生成，测试 complete/incomplete 语义和暂停按钮，不把计划总数误作模型调用分母。

## 14. 可直接拆成开发任务的迁移计划

### 14.1 依赖顺序

`R0 冻结回归 → R1 契约与公共执行核心 → R2 本体/记录/元数据 → R3 语义证明与身份 → R4 调度与恢复 → R5 发布适配 → R6 页面接入 → R7 真实验收与灰度`。

R2 与 R3 可在 R1 契约稳定后并行；R5 的设计可提前，但不得在 R3/R4 门禁未完成前发布新候选。每个任务包单独提交、独立测试，禁止一个提交同时改本体、模型权重、评测参考与调度器。

| 任务包 | 主要改动点 | 交付物与退出条件 |
|---|---|---|
| R0：基线和反例固定 | 现 evaluation 工具、09 归档、相关测试 | 固定当前代码/模型/本体/摘要身份，列清既有能力与缺陷；新增开发用正反例但不改 09 银标；保存版本兼容用旧 checkpoint。 |
| R1：协议与执行核心 | schemas/evidence、extraction_tasks、citation_protocol、hierarchical_context | 新目标/Decision/Proof 契约；抽出 TaskExecutor 与原子协议；旧入口委托核心后原行为回归通过；生产不 import evaluation。 |
| R2：本体/元数据/记录 | ontology_plan、records、metadata、retrieval | 增强只读本体快照的定义来源/约束；动态直接菜单；MetadataSnapshot；物理 mention/逻辑记录分离；所有谓词计划守恒，路由阴性仍在第二阶段。 |
| R3：证明与身份 | verification、resolution、dependencies | 类型/局部共指/全局身份分开；当前谓词桥接检查；N/A、层级、错误 true 与两个来源号反例通过；不能采用一律拒绝策略。 |
| R4：调度与运行状态 | scheduler、ledger、runner factory、annotation_execution/journal 适配 | 新 runner 独立运行；拒绝后 phase2 真实执行器测试；同义重复、循环、多路径、暂停恢复/owner 丢失测试；完整进度 DTO。 |
| R5：候选发布 | publication、candidate_store、fact_commit、版本化 codec、models/evidence 及数据库迁移 | 源/目标版本双向追踪；新增 review_version；完整批次闭包、提交重验，重复发布幂等、算法增量 CAS、人审保护、事务失败恢复；shadow 无候选/事实写入。 |
| R6：应用入口与页面 | api/extraction、api/evidence、document-analysis-panel、evidence-graph、审核界面 | 摘要前置只发生于显式后台任务；GET 无副作用；双协议读取，候选图和有效路径分开；新模式不自动审核。 |
| R7：评测与启用 | cmc_benchmark 适配、quality/rejection/graph 审计、部署配置 | 同一新核心供生产和评测使用；通过第 15 节独立门禁后才从 shadow 到 pilot_review，最终启用需审批。 |

### 14.2 首个可交付切片

第一张实现工单应是 R0/R1，不是“把 QualityGuidedRunner 接入页面”。最小纵向切片包含：

1. 单个文档根、一个直接关系、两个章节记录；首记录类型或关系拒绝，次记录有真实明确关系。
2. 实体无全局编号，但类型和局部指称可靠；验证通过后展开一项属性。
3. 使用真实 IR、原子引用和执行器；模型回调可模拟，但所有证据门禁和调度真实执行。
4. 输出新目标/判定/证明、两阶段 ledger 和 checkpoint；恢复后不重做已完成任务。
5. 只写隔离运行产物，不触达 CandidateStore 或事实提交。

之后加入 CMCReport 两跳正反例、共享表格 mention 与归并，再处理生产发布。这样每一步都有可验证的行为边界，不依赖一次性重写全部 API。

### 14.3 删除与保留范围

保留：原文 IR、结构解析、tokenizer/本地模型调度、安全预算、单位规范化、原文回放、执行所有权、人工审核/快照机制以及已有回归。

替换：全局预展开类型菜单、各处自建的验证上下文、单布尔身份决策、仅双端重叠的关系通过条件、focus-only 的完整分期、隐式 progress/checkpoint 约定。

迁移后才退役：evaluation 中重复的运行算法；只保留兼容导入/旧试验回放和评测薄适配。新旧双轨期间不删除历史实现/冻结目录，不恢复被退役的模板特定抽取器。

## 15. 验收矩阵、真实评测与发布门禁

### 15.1 自动化机制验收

以下测试名称为建议工单名；新增测试放入对应 extraction/API/frontend 测试目录，不冒充当前已执行。

| 编号 | 用例及输入 | 必须断言的结果 |
|---|---|---|
| T01 | CMCReport 根 + 多跳本体 | 首次召回只见当前关系合法 range；尚未关联对象的二跳菜单不进入全文类型召回。 |
| T02 | 继承属性、多个 range 子类、未声明 IRI | 原约束及定义来源保留；并列约束不误作 union；合法子类批次不丢失；未知 IRI 拒绝。 |
| T03 | 摘要称“含 API”，原文没有组成 | 可提高检索顺序，不能产生桥接证据或事实。 |
| T04 | 路由零分/阴性；第一阶段全部拒绝 | 第二阶段的不同记录实际被调用；全部计划记录守恒，不只断言 queue 存在。 |
| T05 | 类型可靠但无唯一编号 | 保留文档局部实体；合法关系/属性可通过；不生成全局身份链接。 |
| T06 | API 表头+名称+试验“来源”编号 | API 类型可支持；把来源号当 API 全局身份的提议不可支持。 |
| T07 | 两行共享 API 合并 cell，来源号不同 | 只记录一处物理 mention 的重复观察；不按试验数创建两个已确认实体，不按同名强合并。 |
| T08 | 复现 call 50/51 的引用组合 | 名称+表头定位未决；加 row1 来源格定位 row1；两种定位结果都不代替身份/组成判断。 |
| T09 | 只有 Product 代码、API 名称和父 describes | 即使模型给 supported=true，缺少可接受的谓词证明时不得形成有效第二跳。另测模型谎报桥接类别的语义评审。 |
| T10 | 明确组成句、配方表、跨段明确指代 | 三种合法形式都能通过，不能只靠提高拒绝率通过 T09。 |
| T11 | Product 属性验证响应讨论 root 或其他对象 | 目标不匹配硬拒绝；ID 正确但语义错目标通过独立角色/谓词核验发现，不能把 ID 校验当全部保护。 |
| T12 | Product 口服片剂与 API 粉末配对例 | 成品外观可通过；不能把原料粉末属性未经证明挂到成品。 |
| T13 | CAS=N/A、布尔=否、字段未提及 | 分别为 placeholder、有效布尔、not_mentioned；不产生 N/A CAS，不吞掉 false。 |
| T14 | binding unsupported、同记录另有合法提议 | 当前提议后续阶段 not_checked；合法兄弟提议和下一个记录仍实际执行。 |
| T15 | 空召回、模型拒答、引用错误、token 超限、超时 | 分别记账；不伪造语义否定；拒答耗尽仍属 attempted_incomplete；换 revision/计划不重置重验额度，失败任务不无限阻塞其他记录。 |
| T16 | false/缺失/错误 true 身份状态 | false/缺失不泄露为可信 key；错误 true 仍须角色/唯一性门禁；router/shared/cache 同样测试。 |
| T17 | 名称+代码局部共指，跨制剂/批次同项目 | 前者可局部归并而全局身份未决；后者不因项目号相同跨层级合并。 |
| T18 | 合并、拆分、晚到竞争主体和第三个模糊节点 | 版本/证明/范围闭包同步；拆分不复制旧属性；新 owner 触发读取范围订阅；冲突不能借传递链被“洗成”一致。 |
| T19 | 根启动、父边审核拒绝、多条独立根路径、本体循环 | 给定根无入边可启动但普通实体不能冒用；依赖路径阻断；合法替代路径可重算；独立根分支继续；无无限递归。 |
| T20 | 同一版本软暂停恢复、owner 丢失、跨版本 checkpoint | 稳定识别 ID 不变而租约 token 更换；同版本幂等恢复；旧 owner 不写；跨版本明确拒绝且旧断点未删。 |
| T21 | Product run revision2 初次发布到 public revision1 | subject/object/path_root/scope/dependency/binding 全部映射正确，无 stale_dependency 或孤儿。 |
| T22 | 后续归并发布、人审修改/拒绝、重复批次和中途故障 | 内容/审核版本双 CAS、人工 head 保留、不能换 alias 绕审；V2 提交重验且不能降格绕过；幂等且事务一致。 |
| T23 | shadow/试点模式、GET 页面、旧作业 | shadow 不写候选/事实；试点 pending 不自动 confirmed；GET 不调用模型；旧行为不变。 |
| T24 | 被拒绝边仍在候选树、全图有待办 | 展示连通与有效路径区分；覆盖和阶段数真实，不能显示全文准确识别完成。 |

复用现有测试：[两跳执行器](../backend/tests/test_extraction/test_evaluation_cmc_product_api_path.py)、[拒绝续检](../backend/tests/test_extraction/test_evaluation_quality_rejection_continuation.py)、[身份隔离](../backend/tests/test_extraction/test_identity_context_quarantine.py)、[实例注册](../backend/tests/test_extraction/test_instance_registry.py)、[分期检索](../backend/tests/test_extraction/test_staged_retrieval.py)、[审核恢复门禁](../backend/tests/test_extraction/test_evaluation_quality_review_gate.py)、[拒绝审计](../backend/tests/test_extraction/test_rejection_analysis.py)及已有引用/表格/图导出测试。迁移测试需加入真实数据库事务与前端集成，不以全是模拟响应的单元测试代替。

### 15.2 独立评测数据

建立独立版本的标注包，至少标注 mention、类型、局部共指、标识角色/作用域、属性 owner/层级、关系方向/桥接、极性/条件以及允许的等价来源集合。将“原文支持/不支持/资料不足或专家有分歧”分开。

09 的 57 项 assistant_silver 和原评分器继续原样使用作历史对照。新标注包不能覆盖该文件；需在新实验前由业务专家复核并冻结。新的实体/关系标注可以接受多处等价原文证明，避免把 paragraph:34 与 paragraph:33 的来源差异误当语义差异；该评分器版本须独立报告，不能与旧指标拼成提升百分比。

新增至少三个场景族：明确 Product→API 组成的正例、同报告同名但没有组成桥接的反例、原料/制剂/批次及试验编号混合的难例。按文档及模板族划分开发/验证/保留测试；当前 CMCReport 及其近重复文档只用于开发回归，不能作为保留集。

### 15.3 运行设计

1. 用新冻结代码重建候选，不导入 08/09 的成功 Product、API 或关系；本体保持既有版本。
2. 先完成机制正反例，再对真实焦点路径运行；取得正例路径见证与通过关键反例是两个验收条件，不能互相替代。
3. 目标文档必须实际进入第二阶段并记录真实调用；若软暂停，只报告本段观察，不补记为全图完成。
4. 随后以 document_graph 模式覆盖 CMCReport 的其他根谓词；设备/计划等分支也各自统计检索、提议、拒绝和待办。
5. 结构-only 与结构+摘要使用同一新验证核心、同一模型/预算做消融，检验摘要增量。另设 GLiNER 消融；新核心与旧核心的比较单列，不能把验证器变化误报为摘要收益。
6. 模型输出存在波动时，对关键正反例安排至少三个独立新 run，记录温度/随机种子（若服务支持）及实际波动；不挑选最好一次。共享服务排队单列。

现有目标文档若仍没有足够原文组成证据，正确结果可以是“发现 Product/API，但该组成关系未决”。这证明安全拒绝的一部分，不证明正例召回成功；正例能力必须由另一个有真实组成证据的文档验证，不能为凑链路补写原文或改本体。

### 15.4 指标与硬门

| 验收面 | 必报内容 | 首期硬门 |
|---|---|---|
| 运行完整性 | 输入/模型/本体/摘要/政策 hash、finalized、原件/副本核验 | 不混版本；正式评分前无活动 marker；原始输入和输出未改。 |
| 工程机制 | 引用回放、权限、版本、计划守恒、幂等、人工保护、模式写入隔离 | T01—T24 对应确定性/执行契约无失败；没有跨作业写入、陈旧引用或摘要事实泄漏。 |
| 语义正反例 | 类型、标识角色、组成桥接、owner/层级、拒绝理由与实际目标 | 预注册关键反例不得产生已接受错误事实；正例必须有独立来源支持的输出。任一关键失败阻断灰度。 |
| 实体/归并 | mention/类型 P/R/F1，局部共指、错误合并/拆分、全局身份精度 | 不以少节点数作为归并优化；关键来源号反例不得升级 API 身份。 |
| 属性/关系 | 完整 tuple：主体、谓词、值/对象、方向、极性、条件、层级 | 双端可回放但谓词不受支持的预测不得计正确；未决和 unscored 单列。 |
| 多跳 | 同一 run 的正确端点、每条边的独立证明、产品属性、根可达率 | 至少有独立真阳性两跳+属性见证；目标文档证据不足不能伪造见证。 |
| 覆盖 | 每谓词计划/已尝试/未完成/未尝试、phase2、未展开前沿、范围外分支 | 声称范围内执行完成时台账与实际 trace 一致；技术失败和上限截断不能算完成。 |
| 成本 | 首个有效结果、模型/非模型/排队/摘要成本、tokens、调用数 | 仅记录；本轮不设必须快于 640.99 秒的门槛。 |

生产准确率/召回率的数值 SLO 在新金标规模及业务风险明确后，由业务与技术共同预注册；本文件不虚构已经得到统计支持的 95%/99% 指标。未批准 SLO 或无保留集时只能进入受控人工审核试点，不能宣称全量质量达标。模型重复试验、关键零容忍反例和测试通过也不能给出总体零错误保证。

### 15.5 发布与回滚

上线顺序：公共核心契约测试 → 隔离真实运行 → shadow 双轨 → 指定文档 pilot_review → 审核政策获批后 enabled。每一步都保留旧 runner 和只读旧数据展示。

发现摘要被用作事实、错误身份角色通过、缺桥接组成被接受、版本引用错配、绕过人工审核、shadow 写候选/事实等任一关键问题，停止新模式接单，暂停在途运行并保存断点，回退新作业到 legacy。不得用回滚删除新运行日志或原地改写已提交事实；涉及已提交事实时走既有纠错/审核快照流程。

## 16. 重构完成的定义与交接清单

“完成重构”需同时满足：

- 新生产编排以当前主体直接本体菜单工作，未预展开多跳类型到全文候选菜单。
- 结构/摘要、检索、原文授权、角色/身份/谓词证明、调度、发布各有独立契约与可测试边界。
- 所有范围内属性/关系都保留补充召回；拒绝后的真实执行和真实第二阶段有日志见证。
- 原子引用、局部无编号实体、表格共享 mention、身份角色、跨记录桥接及实体层级的配对正反例通过。
- 生产 ID/revision 映射、人审保护、API/前端双版本、快照/租约/恢复和回滚经过集成测试。
- 生产和评测使用相同新核心，历史冻结实验保持可回放；本体与新金标治理分开。
- 真实质量按新金标与保留集报告，明确未完成、未决和范围外内容，不把结构图、银标 TP 或全部拒绝当成功。

交付物应包括：模块变更清单、契约/schema 版本说明、数据库与旧协议兼容说明、R0—R7 工单及测试结果、新旧运行 manifest/trace/coverage、独立来源审阅、图投影、发布映射、灰度开关及回滚记录。当前文档只提供这套实施设计，尚未执行其中的重构、数据库迁移或新一轮模型评测。

### 参考入口

- [原方案及 09 后质量边界](基于文档结构和摘要元数据的关系图谱识别方案.md)。
- [质量优先实测报告第 7 节](CMCReport质量优先图谱识别实测报告.md#7-后续提取与拒绝处理复验09专项观察完成)。
- [09 原始归档、统计与完整性核验](evaluations/cmc-product-api-staged-20260908-09/README.md)。
- [09 不读取银标的独立原文审阅](evaluations/cmc-product-api-staged-20260908-09/quality-01/focused-source-review.md)。
- [评测环境与使用边界](../backend/app/evaluation/README.md)。
