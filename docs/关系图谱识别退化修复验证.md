**关系图谱识别退化修复验证（2026-09-07）**

适用模板：`f2ca301f-01f1-412f-a85f-59944445bb09`（风险评估文档 v2.2）。原诊断证据保存在 `/tmp/ontology-f2ca-regression-analysis.md`。

已修复关系进入对象属性任务时的证据范围丢失。对象范围现在保留原始提及，并以已验证肯定关系的原文与独立绑定引用的交集为依据，检索同一语句中的属性。引用对象的 ID 和 revision 必须一致；否定、未验证、已拒绝或过期关系不能扩大范围。语句边界区分中文句号、分号与英文句末，保留小数。缺口补抽也沿指定根对象和关系路径传递依赖证据。

实体召回后增加独立类型判定，拒绝的类型不会进入主体队列。表格同行及对应列头作为类型和绑定上下文提供，保留各单元原始坐标。验证批次同时受输出预算约束，避免只返回部分判定；缺项、重复或未知候选编号均不会授权实体。相同证据提及按候选 ID 去重，文档路径建立后停止该对象的独立任务流。不同原文提及的同名对象仍保留独立身份，跨提及的语义实体归并尚未实现。

调度按 2:1 轮转文档根/关联对象与未关联主体，并先完成模板声明的谓词路径。该模板解析出 15 条优先路径，包含 `hasProductionPlan → plannedBatchSizeMin_kg / plannedBatchSizeMax_kg / productionPurpose`。模板选择、路径及模板内容哈希在重跑时冻结；续跑沿用同一配置。

实际模型验证中同时修复了三类协议遗漏：上下限按当前谓词选取单个端点；引用由程序唯一匹配定位；绑定判定必须携带原文引用。正文任务的模型输出契约排除表格绑定方法；记录坐标由已核对锚点生成，有歧义的映射仍会被拒绝。一个断言失败时，其已独立验证的同批断言得以保留。字面量增加中文“是/否”及 `xsd:gYearMonth`，年月保持原始精度。

任务默认上限由 256 提升至 2048，覆盖后端 Settings、TaskBudget 和 Compose 默认值。标注缓存版本升级为 28；模型上下文、引用协议和调度身份一并升级。旧版任务须重新识别才能产生新结果。

后端容器已重新创建，运行环境实测 `evidence_max_tasks=2048`、`TaskBudget.max_tasks=2048`、标注版本 28；入口 `/api/health` 返回正常，10 个本体模块加载完成，运行中的重跑接口包含 `template_id` 参数。重启前保存了本体存储副本，数据库迁移版本仍为 `0027_report_output_semantics`。

真实模型验收使用本机固定的 `Qwen3.6-35B-A3B`，GGUF revision 为 `0b21525e972670ed59e1812e170b27c26355381f0656ecc4e25617ece7dac58b`，通过同一服务的 tokenizer 计数。在隔离本体存储中读取实际文档 IR，从生产计划段落重新召回对象、验证类型、提取关系并验证属性。

| 文档 | 原始批量 | 下限 | 上限 | 生产年月 | 用途 |
| --- | --- | --- | --- | --- | --- |
| HRS-1597，任务 `e8b24ffa-9fbc-40e1-987f-9585d9b42676` | `3.8- 6.6kg` | `3.8` | `6.6` | `2026-06` | 用于Ⅰ期临床 |
| HRS-5678，任务 `19e291e5-f344-42eb-b5e0-ff40ae3e760b` | `1.50~12.00kg` | `1.50`（规范值 `1.5`） | `12.00`（规范值 `12`） | `2026-02` | 用于临床I期试验 |

工程回归覆盖两个计划的不同批量、防止跨语句串值、无效关系扩大范围、类型误判和不完整判定、输出预算分批、断点恢复、模板优先级冻结、缺口补抽、原文引用协议、字面量、候选持久化及报告取值。相关后端测试累计 498 项通过；前端请求测试 11 项通过，TypeScript 检查和相关 Python Ruff 检查通过。

对 HRS-1597 原任务的六个实际实体候选另做独立类型复核：`HRS-1597` 获得支持；`国药集团化学试剂有限公司`、`约300kg`、`原料厂区大浦仓库`、`囊式滤芯` 和 `AR` 均拒绝归为原料药。判定引用了原始表格中的生产商、批耗量和规格角色，结果保存在 `/tmp/ontology-type-verification-final.json`。

另以原任务中只锚定封面标题的生产计划实体、原任务已经验证的 `hasProductionPlan` 关系重放属性阶段，两个端点 `3.8` / `6.6` 均通过绑定验证。这直接覆盖了诊断中的跨章节失败链，结果为 `/tmp/ontology-original-scope-verification-final.json`，原始请求/响应为 `/tmp/ontology-original-scope-trace-final.json`。

验收范围为目标段落及指定属性，未完成两份文档全部本体路径的端到端质量统计。隔离验证未修改原任务候选或自动确认、提交事实。最终字段结果为 `/tmp/ontology-batch-verification-final.json`，请求及响应为 `/tmp/ontology-batch-trace-hrs1597-final.json` 与 `/tmp/ontology-batch-trace-hrs5678-final.json`；脚本为 `/tmp/ontology-verify-batch-discovery.py`。其余中间调试文件包含失败，不能混作最终结果。
